# Story 1.12: Computer Account Lookup

## Status: Done

## Story

**As a** support technician,
**I want** to search for a computer account and see its details,
**so that** I can diagnose workstation issues.

## Acceptance Criteria

1. Search bar accepts computer name with exact and partial matching.
2. Detail view displays: computer name, DNS hostname, operating system, OS version, last logon timestamp, OU breadcrumb, and enabled/disabled status.
3. Group memberships are displayed in a sortable and filterable DataTable.
4. Ping button sends an ICMP ping and displays reachable/unreachable status with latency in milliseconds.
5. DNS resolution displays the resolved IP address(es) for the computer's DNS hostname.
6. Computer lookup uses the same tab system as user lookups for consistent navigation.

## Tasks / Subtasks

- [x] Create DirectoryComputer model (AC: #2)
    - [x] TypeScript type `DirectoryComputer` in `src/types/directory.ts`
    - [x] `mapEntryToComputer` mapping from DirectoryEntry attributes
    - [x] Fields: name, dnsHostName, operatingSystem, osVersion, lastLogon, distinguishedName, organizationalUnit, enabled, memberOf
- [x] Add search_computers Tauri command (AC: #1)
    - [x] `search_computers` command in `src-tauri/src/commands/mod.rs`
    - [x] Delegates to DirectoryProvider with max 50 results
- [x] Create ComputerLookup page component (AC: #1, #4, #5)
    - [x] Create `src/pages/ComputerLookup.tsx`
    - [x] State machine with LookupState: initial/loading/results/empty/error
    - [x] Search, ping, and DNS handlers via Tauri invoke
    - [x] Auto-select when single result
- [x] Create ComputerLookup layout (AC: #1, #2, #3, #6)
    - [x] SearchBar at top (controlled, debounce=0)
    - [x] Results list in left panel with Monitor icon, name, OS, status badge
    - [x] Detail panel with PropertyGrid (Identity, Status, Location, Network)
    - [x] Group memberships DataTable with FilterBar
    - [x] Loading spinner, empty state, error state with retry
- [x] Implement ping functionality via Tauri command (AC: #4)
    - [x] `ping_host` command using system ping (no new crate dependency)
    - [x] Timeout: 3000ms on Windows, 3s on Unix
    - [x] Returns "Reachable (Xms)" or "Unreachable"
    - [x] Ping button in detail view with loading state
- [x] Implement DNS resolution via Tauri command (AC: #5)
    - [x] `resolve_dns` command using `tokio::net::lookup_host`
    - [x] Returns all resolved IP addresses
    - [x] Auto-resolves when computer is selected via useEffect
- [x] Integrate with tab system (AC: #6)
    - [x] ComputerLookup registered in Sidebar.tsx MODULES array (id="computers")
    - [x] ModuleRouter in App.tsx routes "computers" moduleId to ComputerLookup component
- [x] Write tests (AC: all)
    - [x] ComputerLookup page: 13 tests (search, results, detail, error, retry, ping button)
    - [x] DirectoryComputer mapping: 8 tests (identity, status, OU, memberOf, disabled, missing attrs)

## Dev Notes

### Architecture

ComputerLookup follows the same pattern as UserLookup. The page layout mirrors UserLookup for consistency. Network operations (ping, DNS) are Rust Tauri commands that run asynchronously.

### Rust DirectoryComputer Model

```rust
// src-tauri/src/models/directory_computer.rs
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DirectoryComputer {
    pub name: String,
    pub dns_host_name: String,
    pub operating_system: String,
    pub os_version: String,
    pub last_logon: Option<String>,
    pub distinguished_name: String,
    pub organizational_unit: String,
    pub enabled: bool,
    pub member_of: Vec<String>,
}
```

LDAP attribute mapping:

| Property            | LDAP Attribute                                        |
| ------------------- | ----------------------------------------------------- |
| name                | cn                                                    |
| dns_host_name       | dNSHostName                                           |
| operating_system    | operatingSystem                                       |
| os_version          | operatingSystemVersion                                |
| last_logon          | lastLogon                                             |
| distinguished_name  | distinguishedName                                     |
| organizational_unit | (parsed from DN)                                      |
| enabled             | userAccountControl (bit flag 0x0002 = ACCOUNTDISABLE) |
| member_of           | memberOf                                              |

### Ping Tauri Command

```rust
// src-tauri/src/commands/network.rs
use std::time::Duration;

#[tauri::command]
pub async fn ping_host(hostname: String) -> Result<String, String> {
    // Using surge-ping or similar crate
    match ping_with_timeout(&hostname, Duration::from_millis(3000)).await {
        Ok(latency) => Ok(format!("Reachable ({}ms)", latency.as_millis())),
        Err(_) => Ok("Unreachable".to_string()),
    }
}
```

### DNS Resolution Tauri Command

```rust
#[tauri::command]
pub async fn resolve_dns(hostname: String) -> Result<Vec<String>, String> {
    match tokio::net::lookup_host(format!("{}:0", hostname)).await {
        Ok(addrs) => Ok(addrs.map(|a| a.ip().to_string()).collect()),
        Err(_) => Err("DNS resolution failed".to_string()),
    }
}
```

DNS resolution is triggered automatically from the frontend when `selectedComputer` changes via `useEffect`.

### Frontend Component

```typescript
// src/pages/ComputerLookup.tsx
useEffect(() => {
    if (selectedComputer) {
        invoke<string[]>("resolve_dns", {
            hostname: selectedComputer.dnsHostName,
        })
            .then(setResolvedAddresses)
            .catch(() => setResolvedAddresses(["DNS resolution failed"]));
    }
}, [selectedComputer]);

const handlePing = async () => {
    if (!selectedComputer) return;
    setIsPinging(true);
    try {
        const result = await invoke<string>("ping_host", {
            hostname: selectedComputer.dnsHostName,
        });
        setPingResult(result);
    } catch {
        setPingResult("Unreachable (ping failed)");
    } finally {
        setIsPinging(false);
    }
};
```

### Tab System Integration

ComputerLookup is registered in the navigation context alongside UserLookup:

```typescript
// In NavigationProvider setup
const modules: SidebarModule[] = [
    {
        id: "users",
        label: "User Lookup",
        icon: "user",
        group: "Directory",
        requiredLevel: "ReadOnly",
    },
    {
        id: "computers",
        label: "Computer Lookup",
        icon: "computer",
        group: "Directory",
        requiredLevel: "ReadOnly",
    },
];
```

### Key File Paths

- `src-tauri/src/models/directory_computer.rs`
- `src-tauri/src/commands/computer.rs`
- `src-tauri/src/commands/network.rs`
- `src/pages/ComputerLookup.tsx`
- `src/types/directory.ts`
- `src-tauri/src/services/directory.rs` (add search_computers)

### Testing

- Frontend tests for `ComputerLookup`:
    - Search returns results, updates list
    - Search returns empty, shows empty state
    - Search throws error, shows error state
    - Cancellation of previous search
- Rust tests for ping handling:
    - Mock network layer or use trait abstraction (`NetworkService`)
    - Success: verify "Reachable (Xms)" format
    - Failure: verify "Unreachable" message
- Rust tests for DNS resolution:
    - Mock DNS resolver or use trait abstraction
    - Success: verify IP addresses returned
    - Failure: verify error message
- Rust tests for DirectoryComputer model mapping from LDAP attributes
- Use vitest + React Testing Library for frontend
- Use `cargo test` for Rust backend
- Test naming convention: `describe("ComputerLookup")` / `it("should...")` for frontend; `test_method_scenario_expected` for Rust

## Change Log

| Date       | Version | Description                                              | Author    |
| ---------- | ------- | -------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                         | Romain G. |
| 2026-03-13 | 2.0     | Rewritten for Rust/Tauri v2 + React/TypeScript migration | Romain G. |

## QA Results

### Review Date: 2026-03-13

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

ComputerLookup follows the same well-established pattern as UserLookup, ensuring UI consistency. The ping and DNS resolution commands are correctly implemented using system-level tools. The ComputerDetail component correctly manages ping state and auto-resolves DNS on selection. Type mapping for DirectoryComputer handles UAC bit flags and DN parsing correctly.

### Refactoring Performed

None (batch review - advisory only)

### Fixes Applied (2026-03-13)

- **ping_host async**: Replaced `std::process::Command` with `tokio::process::Command` in `ping_host` to avoid blocking the Tokio runtime thread pool.
- **Tab integration verified**: ComputerLookup is fully integrated into the tab system - registered in Sidebar.tsx MODULES and routed via ModuleRouter in App.tsx. Task checkboxes updated.
- **Rust tests added**: Added 4 Rust tests for ping_host (localhost reachable, invalid host) and resolve_dns (localhost resolution, invalid host error).

### Compliance Check

- Coding Standards: [x] Consistent patterns with UserLookup, proper error handling, TypeScript strict mode
- Project Structure: [x] Page in src/pages/, types in src/types/, commands in src-tauri/src/commands/
- Testing Strategy: [x] 13 ComputerLookup page tests, 8 DirectoryComputer mapping tests, 4 Rust ping/DNS tests
- All ACs Met: [x] All 6 ACs met including AC6 tab integration

### AC Verification

| AC                                        | Status | Notes                                                             |
| ----------------------------------------- | ------ | ----------------------------------------------------------------- |
| #1 Search with exact/partial matching     | PASS   | SearchBar with invoke('search_computers', {query})                |
| #2 Detail view with computer properties   | PASS   | PropertyGrid with Identity, Status, Location, Network sections    |
| #3 Group memberships DataTable            | PASS   | Same DataTable/FilterBar pattern as UserLookup                    |
| #4 Ping button with reachable/unreachable | PASS   | tokio::process::Command ping, non-blocking                        |
| #5 DNS resolution                         | PASS   | tokio::net::lookup_host, auto-resolves on selection               |
| #6 Tab system integration                 | PASS   | Registered in Sidebar.tsx MODULES, routed in App.tsx ModuleRouter |

### Improvements Checklist

- [x] Migrate ping_host from std::process::Command to tokio::process::Command
- [x] Verify AC6 tab system integration (already implemented)
- [x] Add Rust tests for ping and DNS commands
- [ ] Add timeout indicator for DNS resolution
- [ ] Consider caching DNS results for repeated selections

### Security Review

- ping_host passes hostname via args array (safe against shell injection)
- resolve_dns uses tokio::net::lookup_host which is safe

### Performance Considerations

- ping_host now uses tokio::process::Command - non-blocking async execution
- DNS resolution triggered on every computer selection. Consider caching for repeated selections.

### Files Modified During Review

- `src-tauri/src/commands/mod.rs` (updated - ping_host to tokio::process::Command, added 4 tests)
- `docs/stories/1.12.computer-lookup.md` (updated - task checkboxes for AC6)

### Gate Status

Gate: PASS -> docs/qa/gates/1.12-computer-lookup.yml

### Recommended Status

Ready for Done. All 6 acceptance criteria are met. ping_host uses async tokio::process::Command. Tab integration verified. 4 new Rust tests for ping/DNS commands.
