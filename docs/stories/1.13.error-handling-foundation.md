# Story 1.13: Error Handling Foundation

## Status: Draft

## Story

**As a** developer,
**I want** a unified error handling and network resilience layer,
**so that** all AD write operations (starting in Epic 2) fail gracefully with clear user feedback, automatic retry, and structured audit logging.

## Acceptance Criteria

1. A `DSPanelException` base class exists with typed subclasses: `DirectoryException`, `NetworkException`, `PermissionDeniedException`.
2. All `DirectoryProvider` trait LDAP operations wrap raw exceptions into typed `DirectoryException` with user-friendly messages.
3. Transient LDAP/network failures are retried with exponential backoff (1s, 2s, 4s, max 3 attempts) before surfacing the error.
4. A circuit breaker disables a provider after 5 consecutive failures and shows a warning in the notification bar.
5. LDAP connections auto-reconnect after a transient failure (timeout, connection reset) without user intervention.
6. All operations that fail surface a user-friendly error via `NotificationService` trait (not raw exception messages).
7. A global unhandled exception handler catches unexpected errors, logs them at Error/Fatal level, and shows a generic error notification.
8. Timeout configuration is centralized: LDAP 30s, Graph API 15s, HIBP 5s, WMI 10s.
9. Unit tests cover retry logic, circuit breaker state transitions, exception mapping, and reconnection.

## Tasks / Subtasks

- [ ] Task 1: Create exception hierarchy (AC: #1)
  - [ ] Create `DSPanelException` base class in `Exceptions/`
  - [ ] Create `DirectoryException` with `LdapErrorCode` property and user-friendly message mapping
  - [ ] Create `NetworkException` for transient network failures
  - [ ] Create `PermissionDeniedException` with required vs actual permission level
- [ ] Task 2: Implement retry policy (AC: #3, #8)
  - [ ] Create `RetryPolicy` trait interface with `ExecuteAsync<T>(Func<Task<T>>, cancellation token)` method
  - [ ] Implement `ExponentialBackoffRetryPolicy` with configurable delays (default: 1s, 2s, 4s) and max retries (default: 3)
  - [ ] Only retry on transient exceptions (timeout, connection reset, LDAP_UNAVAILABLE, LDAP_BUSY)
  - [ ] Log each retry attempt at Warning level with attempt number and delay
- [ ] Task 3: Implement circuit breaker (AC: #4)
  - [ ] Create `CircuitBreaker` trait interface with `ExecuteAsync<T>()`, `State` (Closed/Open/HalfOpen), and `Reset()` methods
  - [ ] Implement `CircuitBreaker` with configurable failure threshold (default: 5) and recovery timeout (default: 60s)
  - [ ] On state change to Open, push a warning notification via `NotificationService` trait
  - [ ] On HalfOpen, allow one probe request; on success reset to Closed
- [ ] Task 4: Wrap LdapDirectoryProvider with resilience (AC: #2, #5)
  - [ ] Create `ResilientDirectoryProvider` decorator that wraps `LdapDirectoryProvider`
  - [ ] Apply retry policy to all LDAP operations
  - [ ] Apply circuit breaker to the LDAP connection
  - [ ] Map LDAP-specific error codes to `DirectoryException` subclasses
  - [ ] Implement auto-reconnect: on `LdapError` with connection error, dispose and recreate the `ldap3` connection
- [ ] Task 5: Centralize timeout configuration (AC: #8)
  - [ ] Add timeout settings to `AppSettings` (LdapTimeoutSeconds, GraphTimeoutSeconds, HibpTimeoutSeconds, WmiTimeoutSeconds)
  - [ ] Apply LDAP timeout to `ldap3 connection.Timeout` and LDAP search request time limit
  - [ ] Register timeouts in DI so services can access them
- [ ] Task 6: Global exception handler (AC: #7)
  - [ ] Register `Tauri error handler` handler in `App.tsx`
  - [ ] Register `Tokio unhandled task panic hook` handler
  - [ ] Log at Fatal level with full stack trace
  - [ ] Show generic error notification to user (never raw exception text)
  - [ ] Prevent app crash for non-critical exceptions
- [ ] Task 7: User-facing error notifications (AC: #6)
  - [ ] Create helper method `ShowError(Exception, string operationContext)` that maps exceptions to notification severity and user-friendly text
  - [ ] Integrate into existing React components: replace `catch (Exception)` + log with `catch` + `ShowError` + log
- [ ] Task 8: Write tests (AC: #9)
  - [ ] Test ExponentialBackoffRetryPolicy: succeeds after transient failure, gives up after max retries, does not retry non-transient exceptions
  - [ ] Test CircuitBreaker: opens after threshold failures, rejects calls when open, transitions to half-open after timeout, closes on successful probe
  - [ ] Test ResilientDirectoryProvider: retry on timeout, circuit breaker triggers, exception mapping (LDAP error codes to DirectoryException)
  - [ ] Test auto-reconnect: connection lost then recovered
  - [ ] Test global exception handler does not crash the app

## Dev Notes

### Architecture Pattern

The resilience layer uses the **Decorator pattern**: `ResilientDirectoryProvider` wraps the existing `LdapDirectoryProvider` and adds retry + circuit breaker + exception mapping without modifying the original class. DI registration changes from:

```typescript
register_singleton<DirectoryProvider trait, LdapDirectoryProvider>();
```

to:

```typescript
register_singleton<LdapDirectoryProvider>();
register_singleton<DirectoryProvider trait>(sp =>
    new ResilientDirectoryProvider(
        get_service<LdapDirectoryProvider>(),
        get_service<RetryPolicy trait>(),
        get_service<CircuitBreaker trait>(),
        get_service<NotificationService trait>(),
        get_service<ILogger<ResilientDirectoryProvider>>()));
```

### LDAP Error Codes to Handle

| LDAP Error | Code | Classification | Action |
|-----------|------|---------------|--------|
| LDAP_UNAVAILABLE | 52 | Transient | Retry + reconnect |
| LDAP_BUSY | 51 | Transient | Retry |
| LDAP_TIMEOUT | 85 | Transient | Retry + reconnect |
| LDAP_SERVER_DOWN | 81 | Transient | Retry + reconnect |
| LDAP_CONNECT_ERROR | 91 | Transient | Retry + reconnect |
| LDAP_INSUFFICIENT_RIGHTS | 50 | Permanent | Throw PermissionDeniedException |
| LDAP_NO_SUCH_OBJECT | 32 | Permanent | Throw DirectoryException("Object not found") |
| LDAP_CONSTRAINT_VIOLATION | 19 | Permanent | Throw DirectoryException(user-friendly message) |

### Source Tree

```
src-tauri/src/
  Exceptions/
    DSPanelException.rs
    DirectoryException.rs
    NetworkException.rs
    PermissionDeniedException.rs
  Services/
    Resilience/
      RetryPolicy.rs
      ExponentialBackoffRetryPolicy.rs
      CircuitBreaker.rs
      CircuitBreaker.rs
    Directory/
      ResilientDirectoryProvider.rs

src-tauri/tests/
  Services/
    Resilience/
      ExponentialBackoffRetryPolicyTests.rs
      CircuitBreakerTests.rs
    Directory/
      ResilientDirectoryProviderTests.rs
```

### Testing Strategy

All resilience components are fully testable without LDAP:
- `ExponentialBackoffRetryPolicy` takes a `async closure<Task<T>>` - inject a mock that throws then succeeds
- `CircuitBreaker` is pure state machine logic
- `ResilientDirectoryProvider` takes `DirectoryProvider` trait (mock) + `RetryPolicy` trait + `CircuitBreaker` trait
- Use `async closure<int, cancellation token, Task>` for delay injection (like NotificationService) to avoid real waits in tests

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
| 2026-03-13 | 1.1 | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
