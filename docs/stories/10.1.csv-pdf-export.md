# Story 10.1: Multi-Format Export (CSV, PDF, XLSX, HTML)

## Status: Review

## Story

**As a** DSPanel operator,
**I want** to export any list or table view to CSV, PDF, XLSX, or HTML,
**so that** I can share data with stakeholders, auditors, and managers in the format they expect.

## Acceptance Criteria

1. An export button is available on all list and table (React data table) views throughout the application.
2. CSV export produces a UTF-8 file with BOM, with configurable delimiter (comma, semicolon, tab).
3. PDF export includes a title header, generation timestamp, page numbers, and properly formatted table data.
4. XLSX export produces a workbook with a formatted table, auto-filters on columns, and a sheet name matching the view name.
5. HTML export produces a self-contained styled file (inline CSS) suitable for browser viewing or email attachment.
6. A file save dialog opens with a suggested filename based on the view name and current date.
7. A progress indicator is shown for large exports (1000+ rows).
8. Unit tests cover CSV formatting, PDF generation, XLSX output, HTML output, and the export service interface.

## Tasks / Subtasks

- [x] Task 1: Create `ExportService` trait interface (AC: #1)
    - [x] Define `export_to_csv(data, columns, file_path, csv_options)` method
    - [x] Define `export_to_pdf(data, columns, file_path, title)` method
    - [x] Define `export_to_xlsx(data, columns, file_path, sheet_name)` method
    - [x] Define `export_to_html(data, columns, file_path, title)` method
    - [x] Create `ColumnDefinition` model (header, property name, width hint)
    - [x] Create `CsvOptions` model (delimiter, include headers flag)
- [x] Task 2: Implement `CsvExportService` (AC: #2)
    - [x] Use csv crate for CSV generation
    - [x] Configure UTF-8 encoding with BOM
    - [x] Support configurable delimiter (comma, semicolon, tab)
    - [x] Map column definitions to csv crate class maps
- [x] Task 3: Implement `PdfExportService` (AC: #3)
    - [x] Use printpdf crate with built-in Helvetica font
    - [x] Add title header with report name
    - [x] Add generation timestamp in header
    - [x] Auto-pagination when content exceeds page height
    - [x] Render data as table with column headers and row data
    - [x] Handle column width distribution based on content
- [x] Task 4: Implement `XlsxExportService` (AC: #4)
    - [x] Use rust_xlsxwriter crate for XLSX generation
    - [x] Create worksheet with sheet name matching view name
    - [x] Write header row with bold formatting
    - [x] Write data rows with appropriate column types (text, number, date)
    - [x] Add auto-filters on all columns
    - [x] Auto-fit column widths based on content
- [x] Task 5: Implement `HtmlExportService` (AC: #5)
    - [x] Generate self-contained HTML with inline CSS (no external dependencies)
    - [x] Include title header with report name and generation timestamp
    - [x] Render data as a styled HTML table with striped rows
    - [x] Ensure proper UTF-8 encoding and HTML escaping
- [x] Task 6: Integrate export button in all React data table/list views (AC: #1, #6)
    - [x] Create reusable `ExportToolbar` React component with CSV, PDF, XLSX, and HTML buttons
    - [x] Add to existing React data table views (UserDetail, ComputerDetail, GroupHygiene, ReplicationStatus)
    - [x] File save dialog with suggested name: `{ViewName}_{yyyy-MM-dd}.{ext}`
- [x] Task 7: Progress indicator (AC: #7)
    - [x] Show "Exporting..." text state during export
    - [x] Export runs asynchronously via Tauri command (non-blocking)
    - [x] Disable export button while export is in progress
- [x] Task 8: Write tests (AC: #8)
    - [x] Test CSV output format (UTF-8 BOM, delimiter, headers)
    - [x] Test PDF generation produces valid document
    - [x] Test XLSX output (worksheet name, auto-filters, column types)
    - [x] Test HTML output (valid markup, inline CSS, UTF-8, escaped content)
    - [x] Test column mapping for various data types
    - [x] Test large dataset export completes successfully

## Dev Notes

### File Layout (Actual)

- `src-tauri/src/services/export.rs` - Export functions (CSV, PDF, XLSX, HTML) + models + 27 unit tests
- `src-tauri/src/commands/storage.rs` - `export_table` Tauri command
- `src/components/common/ExportToolbar.tsx` - Reusable export dropdown component
- `src/components/common/ExportToolbar.test.tsx` - 10 unit tests

### Crate Dependencies

- `csv` 1.x - CSV generation (already present)
- `printpdf` 0.3 - PDF generation with built-in Helvetica font
- `rust_xlsxwriter` 0.82 - XLSX generation (pure Rust)
- HTML export uses standard string templating (no extra crate)

### Integrated Pages

- `src/pages/UserDetail.tsx` - Group memberships table
- `src/pages/ComputerDetail.tsx` - Group memberships table
- `src/pages/GroupHygiene.tsx` - Combined hygiene findings
- `src/pages/ReplicationStatus.tsx` - Replication partnerships table

### Testing

- 27 Rust unit tests in `src-tauri/src/services/export.rs` (CSV, HTML, PDF, XLSX, large datasets)
- 10 TypeScript unit tests in `src/components/common/ExportToolbar.test.tsx`

## Change Log

| Date       | Version | Description                                                            | Author    |
| ---------- | ------- | ---------------------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                                       | Romain G. |
| 2026-03-13 | 1.1     | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
| 2026-03-24 | 2.0     | Added XLSX and HTML export formats, renamed story, updated tasks       | Romain G. |
| 2026-03-24 | 3.0     | Implementation complete: export service, ExportToolbar, page integrations | Romain G. |
