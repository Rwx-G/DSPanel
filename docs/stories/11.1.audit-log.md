# Story 11.1: Internal Audit Log

## Status: Draft

## Story

**As a** DomainAdmin,
**I want** a searchable audit log of all write operations performed through DSPanel,
**so that** I can track who did what, when, and investigate issues or policy violations.

## Acceptance Criteria

1. A dedicated audit log view displays a searchable, filterable table with columns: timestamp, user, action, target, details, and result.
2. All write operations across the application (password resets, account modifications, group changes, cleanup actions) feed into the audit log via `IAuditService`.
3. Audit entries are stored in a local SQLite database.
4. Filters are available for: date range, user, action type, and target DN (partial match).
5. The audit log supports CSV and PDF export.
6. Configurable retention period (default 1 year) with automatic cleanup of older entries.
7. Unit tests cover the audit service, repository, ViewModel filtering, and retention cleanup.

## Tasks / Subtasks

- [ ] Task 1: Create `AuditLogEntry` model (AC: #1)
  - [ ] Properties: Id, Timestamp, UserName, ActionType, TargetDn, Details, Result
  - [ ] Create `ActionType` constants or enum for known action types
- [ ] Task 2: Create `IAuditService` interface (AC: #2)
  - [ ] Define `LogAsync(string actionType, string targetDn, string details, string result)`
  - [ ] Define `QueryAsync(AuditLogFilter filter)` returning paginated results
  - [ ] Define `PurgeOlderThanAsync(DateTime cutoff)`
- [ ] Task 3: Implement `AuditService` with SQLite + Dapper (AC: #2, #3)
  - [ ] Create SQLite database and `audit_log` table on first use
  - [ ] Implement insert with current timestamp and authenticated user
  - [ ] Implement filtered query with Dapper
  - [ ] Handle concurrent writes safely
- [ ] Task 4: Create `AuditLogViewModel` (AC: #1, #4)
  - [ ] Observable collection of `AuditLogEntry`
  - [ ] Filter properties: DateFrom, DateTo, UserFilter, ActionFilter, TargetFilter
  - [ ] Search command that re-queries with current filters
  - [ ] Pagination support
- [ ] Task 5: Create `AuditLogView.xaml` (AC: #1, #4)
  - [ ] Filter bar with date pickers, text inputs, and search button
  - [ ] DataGrid with sortable columns
  - [ ] Detail panel on row selection
- [ ] Task 6: Export (AC: #5)
  - [ ] CSV and PDF export buttons using `IExportService`
- [ ] Task 7: Retention cleanup (AC: #6)
  - [ ] Configurable retention period in settings (default 365 days)
  - [ ] Run purge on application startup
  - [ ] Log purge action itself
- [ ] Task 8: Ensure all services call `IAuditService` (AC: #2)
  - [ ] Review all write operations across the codebase
  - [ ] Add `IAuditService.LogAsync()` calls where missing
- [ ] Task 9: Write tests (AC: #7)
  - [ ] Test audit entry insertion and retrieval
  - [ ] Test filtered queries return correct results
  - [ ] Test retention purge deletes old entries
  - [ ] Test ViewModel filtering and pagination
  - [ ] Test all write services call `IAuditService`

## Dev Notes

### Database Schema

```sql
CREATE TABLE audit_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    user_name TEXT NOT NULL,
    action_type TEXT NOT NULL,
    target_dn TEXT,
    details TEXT,
    result TEXT NOT NULL
);

CREATE INDEX idx_audit_timestamp ON audit_log(timestamp);
CREATE INDEX idx_audit_action ON audit_log(action_type);
CREATE INDEX idx_audit_user ON audit_log(user_name);
```

### File Layout

- `Services/Audit/IAuditService.cs`
- `Services/Audit/AuditService.cs`
- `Data/AuditRepository.cs`
- `Models/Audit/AuditLogEntry.cs`
- `Models/Audit/AuditLogFilter.cs`
- `ViewModels/Audit/AuditLogViewModel.cs`
- `Views/Audit/AuditLogView.xaml`

### Testing

- Use in-memory SQLite for repository tests.
- Use `Moq` to mock `IAuditService` in ViewModel tests.
- Use `FluentAssertions` for readable assertions.
- Test file: `src/DSPanel.Tests/Services/Audit/AuditServiceTests.cs`
- Test file: `src/DSPanel.Tests/Data/AuditRepositoryTests.cs`
- Test file: `src/DSPanel.Tests/ViewModels/Audit/AuditLogViewModelTests.cs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
