# Story 11.2: AD Change History Timeline

## Status: Draft

## Story

**As a** DSPanel operator,
**I want** to view the change history of any AD object as a timeline,
**so that** I can see what attributes changed, when, and what the previous values were.

## Acceptance Criteria

1. A "History" tab is available on every AD object detail view (user, group, computer, OU).
2. The timeline displays attribute changes with: attribute name, timestamp, originating DC, old value, and new value.
3. Data is sourced from the `msDS-ReplAttributeMetaData` attribute on each object.
4. The timeline is sortable by date (newest first by default) and filterable by attribute name.
5. Multi-value attributes (e.g., member) show a visual diff highlighting added and removed values.
6. Unit tests cover metadata parsing, timeline construction, and diff visualization logic.

## Tasks / Subtasks

- [ ] Task 1: Parse `msDS-ReplAttributeMetaData` (AC: #3)
  - [ ] Query the attribute via `IDirectoryProvider` (requires constructed attribute request)
  - [ ] Parse the XML format to extract: attribute name, version, originating time, originating DC GUID
  - [ ] Resolve DC GUID to DC hostname
  - [ ] Create `AttributeChangeEntry` model
- [ ] Task 2: Create timeline component (AC: #1, #2)
  - [ ] `ChangeHistoryView.xaml` as a tab within object detail views
  - [ ] Timeline layout with entries ordered by date
  - [ ] Each entry shows attribute name, timestamp, originating DC
- [ ] Task 3: Display attribute change values (AC: #2)
  - [ ] Show current value for each attribute (from the live object)
  - [ ] Note: `msDS-ReplAttributeMetaData` provides version and timestamp but not historical values
  - [ ] Display version count and last change timestamp per attribute
- [ ] Task 4: Filter by attribute (AC: #4)
  - [ ] Attribute name filter dropdown or search box
  - [ ] Sort toggle (newest/oldest first)
- [ ] Task 5: Multi-value diff visualization (AC: #5)
  - [ ] For multi-value attributes, compare snapshots if available (via `ISnapshotService`)
  - [ ] Highlight added values in green, removed values in red
  - [ ] Side-by-side or inline diff view
- [ ] Task 6: Write tests (AC: #6)
  - [ ] Test XML parsing of `msDS-ReplAttributeMetaData`
  - [ ] Test timeline construction and ordering
  - [ ] Test attribute filtering
  - [ ] Test multi-value diff logic

## Dev Notes

### msDS-ReplAttributeMetaData Format

The attribute is a constructed attribute returning XML:

```xml
<DS_REPL_ATTR_META_DATA>
  <pszAttributeName>displayName</pszAttributeName>
  <dwVersion>3</dwVersion>
  <ftimeLastOriginatingChange>2026-02-15T10:30:00Z</ftimeLastOriginatingChange>
  <uuidLastOriginatingDsaInvocationID>GUID</uuidLastOriginatingDsaInvocationID>
  <usnOriginatingChange>12345</usnOriginatingChange>
</DS_REPL_ATTR_META_DATA>
```

Note: This metadata provides the version number and last change timestamp but does not contain historical values. To show before/after values, the application would need to compare with stored snapshots from `ISnapshotService` (if available).

### File Layout

- `Services/History/ChangeHistoryService.cs`
- `Services/History/ReplMetadataParser.cs`
- `Models/History/AttributeChangeEntry.cs`
- `ViewModels/History/ChangeHistoryViewModel.cs`
- `Views/History/ChangeHistoryView.xaml`

### Testing

- Use `Moq` to mock `IDirectoryProvider`.
- Provide sample XML strings for parser tests.
- Use `FluentAssertions` for readable assertions.
- Test file: `src/DSPanel.Tests/Services/History/ReplMetadataParserTests.cs`
- Test file: `src/DSPanel.Tests/ViewModels/History/ChangeHistoryViewModelTests.cs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
