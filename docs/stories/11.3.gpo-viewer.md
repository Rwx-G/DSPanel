# Story 11.3: GPO Viewer

## Status: Review

## Story

**As a** DomainAdmin,
**I want** to view Group Policy Objects linked to users, computers, and OUs,
**so that** I can understand effective policy application and troubleshoot GPO issues without leaving DSPanel.

## Acceptance Criteria

1. A GPO panel is available on user, computer, and OU detail views showing linked and inherited GPOs.
2. Each GPO entry displays: name, link order, enforcement status, and WMI filter (if any).
3. A "what-if" mode simulates which GPOs would apply to a given user at a specified OU.
4. A scope report for a selected GPO shows all OUs where it is linked.
5. The GPO viewer is read-only (no modifications).
6. The view is gated to DomainAdmin permission level.
7. Unit tests cover GPO link parsing, inheritance resolution, and what-if simulation.

## Tasks / Subtasks

- [x] Task 1: Query GPO links via LDAP (AC: #1, #2)
    - [x] Read gPLink attribute from OU, domain objects
    - [x] Parse gPLink value format (services/gpo.rs parse_gp_link)
    - [x] Resolve GPO DNs to display names via search_configuration
    - [x] GpoLink model (gpoName, gpoDn, linkOrder, isEnforced, isDisabled, linkedAt, isInherited)
- [x] Task 2: Create GPO viewer page (AC: #1, #2)
    - [x] GpoViewer.tsx with three tab modes: Links, Scope, What-If
    - [x] Table showing GPO name, link order, container, enforced/inherited status
    - [x] Visual indicators: shield icon for enforced, badges for inherited/direct
- [x] Task 3: Inherited GPO resolution (AC: #1)
    - [x] Walk OU hierarchy from domain root down to object's parent
    - [x] Respect block inheritance (gPOptions=1) and enforcement flags
    - [x] Display effective GPO list with enforced GPOs first
- [x] Task 4: What-if simulation (AC: #3)
    - [x] UI with target OU and optional user DN inputs
    - [x] Simulates GPO application at the specified OU
    - [x] Displays resulting GPO list
- [x] Task 5: Scope report (AC: #4)
    - [x] Enter GPO DN, find all OUs and domain root where it's linked
    - [x] Display containers with enforced/disabled status
- [x] Task 6: Permission gating (AC: #5, #6)
    - [x] DomainAdmin permission required for all GPO commands
    - [x] Read-only: no LDAP write operations
    - [x] Sidebar entry under Infrastructure group
- [x] Task 7: Write tests (AC: #7)
    - [x] gPLink parsing: single, multiple, flags (0/1/2/3), empty, lowercase (7 tests)
    - [x] Inheritance resolution: simple, block, enforced survives block, disabled skip, inherited (5 tests)
    - [x] Frontend: page rendering, tab switching, fetch/display, errors, export (9 tests)

## Dev Notes

### gPLink Attribute Format

The `gPLink` attribute on OU/domain/site objects uses this format:

```
[LDAP://cn={GUID},cn=policies,cn=system,DC=contoso,DC=com;0][LDAP://cn={GUID2},...;2]
```

Link flags:

- `0` = enabled, not enforced
- `1` = disabled
- `2` = enforced
- `3` = disabled and enforced

### GPO Inheritance Rules

1. GPOs are processed from the domain level down to the OU containing the object.
2. Lower OUs override higher OUs (last writer wins) unless the higher GPO is enforced.
3. "Block Inheritance" on an OU stops all non-enforced GPOs from parent OUs.
4. Enforced GPOs always apply regardless of block inheritance.

### File Layout

- `Services/Gpo/GpoService.rs`
- `Services/Gpo/GpLinkParser.rs`
- `Services/Gpo/GpoInheritanceResolver.rs`
- `Models/Gpo/GpoLink.rs`
- `Models/Gpo/GpoInfo.rs`
- `components/Gpo/GpoPanelComponent.tsx`
- `components/Gpo/GpoWhatIfComponent.tsx`
- `Views/Gpo/GpoPanelView.tsx`

### Testing

- Use `mockall` (Rust) or vitest mocks to mock `DirectoryProvider` trait.
- Provide sample `gPLink` strings for parser tests.
- Build synthetic OU hierarchies for inheritance tests.
- Use standard Rust or TypeScript assertions for readable assertions.
- Test file: `src-tauri/tests/Services/Gpo/GpLinkParserTests.rs`
- Test file: `src-tauri/tests/Services/Gpo/GpoInheritanceResolverTests.rs`

## Change Log

| Date       | Version | Description                                                            | Author    |
| ---------- | ------- | ---------------------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                                       | Romain G. |
| 2026-03-13 | 1.1     | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
