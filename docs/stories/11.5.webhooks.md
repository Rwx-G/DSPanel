# Story 11.5: Webhook Notifications

## Status: Draft

## Story

**As a** DomainAdmin,
**I want** DSPanel to send notifications via webhooks and email when specific events occur,
**so that** my team is alerted in real time through our existing communication channels.

## Acceptance Criteria

1. A configuration UI allows defining notification channels: webhook URL (Teams/Slack compatible) and SMTP email.
2. Subscribable events include: account lockout, privileged group membership change, security alert triggered, and automation rule executed.
3. Notification payloads include event type, timestamp, affected object, operator, and details.
4. A "Test" button sends a test notification to verify channel configuration.
5. A notification history log shows past notifications with status (sent/failed), timestamp, and channel.
6. Unit tests cover webhook sending, email sending, event subscription, and history logging.

## Tasks / Subtasks

- [ ] Task 1: Create `NotificationService` trait interface (AC: #1, #2, #3)
    - [ ] Define `SendAsync(NotificationEvent event, IReadOnlyList<NotificationChannel> channels)`
    - [ ] Create `NotificationEvent` model (type, timestamp, targetDn, operator, details)
    - [ ] Create `NotificationChannel` model (type, config - URL or SMTP settings)
    - [ ] Create `NotificationChannelType` enum (Webhook, Email)
- [ ] Task 2: Implement `WebhookSender` (AC: #1, #3)
    - [ ] Use `reqwest` client to POST JSON payload to configured URL
    - [ ] Format payload for Teams/Slack adaptive card or message format
    - [ ] Handle HTTP errors and timeouts
    - [ ] Retry logic (1 retry with exponential backoff)
- [ ] Task 3: Implement `SmtpSender` (AC: #1, #3)
    - [ ] Use `lettre crate` to send email notifications
    - [ ] Configure SMTP server, port, credentials, TLS in settings
    - [ ] HTML email template with event details
- [ ] Task 4: Notification configuration UI (AC: #1)
    - [ ] `NotificationSettingsView.tsx` with channel list and add/edit/delete
    - [ ] Channel editor: type picker, URL or SMTP configuration fields
    - [ ] Event subscription checkboxes per channel
- [ ] Task 5: Event subscription (AC: #2)
    - [ ] Hook into relevant services to raise notification events:
        - Account lockout detection
        - Privileged group change (from Story 9.1)
        - Security alert (from Story 9.3)
        - Automation rule executed (from Story 11.3)
    - [ ] Filter events by channel subscription configuration
- [ ] Task 6: Test notification button (AC: #4)
    - [ ] Send a test event payload to the configured channel
    - [ ] Display success/failure result inline
- [ ] Task 7: History log (AC: #5)
    - [ ] SQLite table: `notification_history (id, timestamp, event_type, channel_type, status, error_message)`
    - [ ] History view with React data table
- [ ] Task 8: Write tests (AC: #6)
    - [ ] Test webhook payload formatting
    - [ ] Test HTTP POST with mock handler
    - [ ] Test SMTP sending with mock client
    - [ ] Test event subscription routing
    - [ ] Test history recording

## Dev Notes

### Webhook Payload Format (Teams/Slack Compatible)

```json
{
    "text": "DSPanel Alert: Account Lockout",
    "sections": [
        {
            "activityTitle": "Account Lockout Detected",
            "facts": [
                { "name": "Account", "value": "john.doe" },
                { "name": "Time", "value": "2026-03-10T14:30:00Z" },
                { "name": "Source", "value": "DC01.contoso.com" }
            ]
        }
    ]
}
```

### SMTP Configuration

```json
{
    "smtp": {
        "server": "smtp.contoso.com",
        "port": 587,
        "useTls": true,
        "username": "dspanel@contoso.com",
        "from": "dspanel@contoso.com",
        "recipients": ["admin@contoso.com"]
    }
}
```

### File Layout

- `Services/Notifications/NotificationService.rs`
- `Services/Notifications/NotificationService.rs`
- `Services/Notifications/WebhookSender.rs`
- `Services/Notifications/SmtpSender.rs`
- `Models/Notifications/NotificationEvent.rs`
- `Models/Notifications/NotificationChannel.rs`
- `Data/NotificationHistoryRepository.rs`
- `components/Notifications/NotificationSettingsComponent.tsx`
- `Views/Notifications/NotificationSettingsView.tsx`

### Testing

- Use `mockall` (Rust) or vitest mocks to mock `reqwest` client (via `reqwest mock handler`) and `SmtpClient`.
- Use in-memory SQLite for history repository tests.
- Use standard Rust or TypeScript assertions for readable assertions.
- Test file: `src-tauri/tests/Services/Notifications/WebhookSenderTests.rs`
- Test file: `src-tauri/tests/Services/Notifications/SmtpSenderTests.rs`

## Change Log

| Date       | Version | Description                                                            | Author    |
| ---------- | ------- | ---------------------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                                       | Romain G. |
| 2026-03-13 | 1.1     | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
