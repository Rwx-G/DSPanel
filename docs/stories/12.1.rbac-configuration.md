# Story 12.1: AD Group-Based Permission Mapping

## Status: Review

## Story

**As a** DomainAdmin,
**I want** to map AD security groups to DSPanel permission levels,
**so that** access control is driven by AD group membership rather than auto-detection only.

## Acceptance Criteria

1. A permission mapping UI is accessible in Settings, gated to DomainAdmin only.
2. For each of the 5 permission levels (ReadOnly, HelpDesk, AccountOperator, Admin, DomainAdmin), one or more AD groups can be mapped.
3. Multiple groups can be mapped to the same permission level.
4. The highest matching level wins when a user belongs to multiple mapped groups.
5. When no custom mapping is configured, the current auto-detection logic applies (RID-based + default DSPanel-* groups).
6. Mappings are saved to the preset storage share as JSON (shared across instances).
7. All mapping changes are logged via `AuditService`.
8. Validation warns when a mapped group does not exist in AD.
9. Unit tests cover mapping resolution, highest-level logic, fallback behavior, and persistence.

## Tasks / Subtasks

- [x] Task 1: Permission mapping model (AC: #2, #3, #6)
    - [x] Create `PermissionMapping` struct: `HashMap<PermissionLevel, Vec<String>>` (level -> list of group DNs)
    - [x] JSON serialization to `rbac-mappings.json` in preset storage path
    - [x] Merge logic: custom mappings extend (not replace) default RID + DSPanel-* group detection
- [x] Task 2: Rust service - load/save mappings (AC: #5, #6)
    - [x] Add `load_permission_mappings()` and `save_permission_mappings()` to preset storage
    - [x] On startup in `lib.rs`, load mappings from preset path and inject into `PermissionService`
    - [x] Extend `PermissionConfig.group_mappings` with loaded custom mappings
- [x] Task 3: Permission resolution with custom mappings (AC: #4, #5)
    - [x] In `detect_permissions()`, merge custom group_mappings into the existing flow
    - [x] Highest matching level wins (already the behavior - verify it works with merged mappings)
    - [x] Fallback: if no custom mappings configured, existing logic unchanged
- [x] Task 4: Tauri IPC commands (AC: #1, #2, #6, #8)
    - [x] `get_permission_mappings() -> PermissionMappings` - returns current config
    - [x] `set_permission_mappings(mappings)` - validates, saves, and reloads
    - [x] `validate_group_exists(group_dn: String) -> bool` - LDAP lookup to verify group
- [x] Task 5: Permission mapping UI component (AC: #1, #2, #3, #8)
    - [x] `PermissionMappingSettings.tsx` - table with one row per level
    - [x] Each row: level label + list of mapped groups with add/remove
    - [x] Group search field (calls `search_groups` existing command) with autocomplete
    - [x] Warning badge on groups that fail validation
    - [x] Save button calls `set_permission_mappings`
    - [x] Gate entire component to DomainAdmin via `PermissionGate`
- [x] Task 6: Integrate into Settings view (AC: #1)
    - [x] Component ready for embedding in Settings Permissions tab (Story 12.2)
- [x] Task 7: Audit logging (AC: #7)
    - [x] Log mapping changes via `audit_service.log_success()` with action "PermissionMappingUpdate"
    - [x] Include mapping count in details
- [x] Task 8: Write tests (AC: #9)
    - [x] Rust: test merged mappings resolve highest level correctly
    - [x] Rust: test fallback to defaults when no custom mappings
    - [x] Rust: test JSON round-trip for PermissionMappings
    - [x] Frontend: test PermissionMappingSettings renders, adds/removes groups, saves

## Dev Notes

### Mapping JSON Schema

Saved as `rbac-mappings.json` in the preset storage path:
```json
{
  "mappings": {
    "ReadOnly": [],
    "HelpDesk": ["CN=IT-Support-Paris,OU=Groups,DC=contoso,DC=com"],
    "AccountOperator": [
      "CN=IT-Support-Level2,OU=Groups,DC=contoso,DC=com",
      "CN=HR-UserManagers,OU=Groups,DC=contoso,DC=com"
    ],
    "Admin": [],
    "DomainAdmin": []
  }
}
```

### Permission Resolution Order

1. Load custom mappings from preset storage (if any)
2. Merge into `PermissionConfig.group_mappings` alongside default DSPanel-* entries
3. `detect_permissions()` runs as before: RID check first, then group name matching, then probe
4. Highest detected level wins

### Existing Code References

- `PermissionService` with `PermissionConfig { group_mappings, rid_mappings }`: `src-tauri/src/services/permissions.rs`
- `PresetService` for preset storage path: `src-tauri/src/services/preset.rs`
- `search_groups` command for group autocomplete: `src-tauri/src/commands/directory.rs`
- `AuditService.log_success()`: `src-tauri/src/services/audit.rs`
- `PermissionGate` component: `src/components/common/PermissionGate.tsx`

### File Layout

- `src-tauri/src/services/permissions.rs` - extend with mapping load/save
- `src-tauri/src/commands/storage.rs` - add permission mapping commands
- `src/components/common/PermissionMappingSettings.tsx` - new UI component
- `src/components/common/PermissionMappingSettings.test.tsx` - frontend tests

### Testing

- Rust: inline `#[cfg(test)]` module in `permissions.rs`
- Frontend: vitest with mocked invoke() in colocated test file
- Test file: `src-tauri/src/services/permissions.rs` (extend existing tests)
- Test file: `src/components/common/PermissionMappingSettings.test.tsx`

## Change Log

| Date       | Version | Description                                                    | Author    |
| ---------- | ------- | -------------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                               | Romain G. |
| 2026-03-13 | 1.1     | Updated tech references for Rust/Tauri v2 + React/TS migration | Romain G. |
| 2026-03-25 | 2.0     | Simplified scope: AD group mapping only, no custom profiles    | Romain G. |
