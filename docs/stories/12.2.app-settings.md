# Story 12.2: Application Settings

## Status: Draft

## Story

**As a** DSPanel user,
**I want** a centralized settings interface organized by category,
**so that** I can configure connection, security, appearance, and other preferences in one place.

## Acceptance Criteria

1. A Settings module is accessible from the sidebar navigation (Settings group).
2. Settings are organized by category tabs: Connection, Presets, Permissions, Security, Reports, Appearance.
3. Connection settings include: domain override, preferred DC, Microsoft Graph configuration (tenant ID, client ID). Graph client secret stored via `CredentialStore` trait (already implemented).
4. Presets settings include: storage path configuration (reuse existing `PresetSettings` component).
5. Permissions tab hosts the permission mapping UI from Story 12.1.
6. Security settings include: audit retention period.
7. Reports settings include: default export format and export directory.
8. Appearance settings include: theme selection (Light, Dark, System).
9. All settings are persisted locally (JSON for non-secrets, OS credential store for secrets) and survive application restarts.
10. Settings are validated before saving (e.g., valid paths, valid URLs, numeric ranges).
11. Unit tests cover settings persistence, validation, defaults, and UI rendering.

## Tasks / Subtasks

- [ ] Task 1: Extend AppSettings struct (AC: #3, #6, #7, #8)
    - [ ] Add `connection` section: `domain_override`, `preferred_dc` (graph fields already exist)
    - [ ] Add `reports` section: `default_format` (CSV/PDF), `default_export_path`
    - [ ] Add `appearance` section: `theme` (light/dark/system)
    - [ ] Preserve backward compatibility with existing settings fields
- [ ] Task 2: Add Settings module to navigation (AC: #1)
    - [ ] Add `{ id: "settings", label: "Settings", icon: "settings", group: "Settings" }` to MODULES in `Sidebar.tsx`
    - [ ] Add `"settings"` -> `Settings` mapping in MODULE_COMPONENTS in `App.tsx`
- [ ] Task 3: Create Settings page with tabs (AC: #2)
    - [ ] `src/pages/Settings.tsx` with tab navigation
    - [ ] Tabs: Connection, Presets, Permissions, Security, Reports, Appearance
    - [ ] Load current settings on mount via `get_app_settings()`
    - [ ] Save button per tab or global save
- [ ] Task 4: Connection tab (AC: #3)
    - [ ] Domain override text field (empty = auto-detect)
    - [ ] Preferred DC text field (empty = auto-select)
    - [ ] Embed existing `GraphSettings` component for Graph configuration
- [ ] Task 5: Presets tab (AC: #4)
    - [ ] Embed existing `PresetSettings` component
- [ ] Task 6: Permissions tab (AC: #5)
    - [ ] Embed `PermissionMappingSettings` component from Story 12.1
    - [ ] Gated to DomainAdmin (show "Access Denied" for lower levels)
- [ ] Task 7: Security tab (AC: #6)
    - [ ] Audit retention period input (days, default 365, min 30)
- [ ] Task 8: Reports tab (AC: #7)
    - [ ] Default export format dropdown (CSV, PDF)
    - [ ] Default export directory with folder picker
- [ ] Task 9: Appearance tab (AC: #8)
    - [ ] Theme selector: Light, Dark, System
    - [ ] Integrate with existing `useTheme()` hook
    - [ ] Persist theme choice in AppSettings (migrate from localStorage)
- [ ] Task 10: Validation (AC: #10)
    - [ ] Validate file paths are accessible
    - [ ] Validate numeric ranges (retention > 0)
    - [ ] Show inline validation errors
- [ ] Task 11: Write tests (AC: #11)
    - [ ] Rust: test AppSettings extended struct serialization round-trip
    - [ ] Rust: test defaults applied when fields missing
    - [ ] Frontend: test Settings page renders all tabs
    - [ ] Frontend: test validation messages appear on invalid input
    - [ ] Frontend: test save calls set_app_settings with correct data

## Dev Notes

### Extended AppSettings JSON Structure

```json
{
  "disabledOu": "OU=Disabled,DC=contoso,DC=com",
  "graphTenantId": "...",
  "graphClientId": "...",
  "privilegedGroups": ["Domain Admins"],
  "cleanupRules": [],
  "auditRetentionDays": 365,
  "connection": {
    "domainOverride": "",
    "preferredDc": ""
  },
  "reports": {
    "defaultFormat": "PDF",
    "defaultExportPath": ""
  },
  "appearance": {
    "theme": "system"
  }
}
```

### Existing Code References

- `AppSettingsService`: `src-tauri/src/services/app_settings.rs`
- `get_app_settings` / `set_app_settings` commands: `src-tauri/src/commands/storage.rs`
- `GraphSettings` component: `src/components/common/GraphSettings.tsx`
- `PresetSettings` component: `src/components/common/PresetSettings.tsx`
- `useTheme()` hook: `src/hooks/useTheme.ts`
- `CredentialStore` trait: `src-tauri/src/services/credential_store.rs`
- Sidebar MODULES: `src/components/layout/Sidebar.tsx`
- MODULE_COMPONENTS: `src/App.tsx`

### File Layout

- `src-tauri/src/services/app_settings.rs` - extend AppSettings struct
- `src-tauri/src/commands/storage.rs` - update settings commands if needed
- `src/pages/Settings.tsx` - new settings page
- `src/pages/Settings.test.tsx` - frontend tests
- `src/components/layout/Sidebar.tsx` - add settings module
- `src/App.tsx` - add settings route

### Testing

- Rust: extend existing `#[cfg(test)]` module in `app_settings.rs`
- Frontend: vitest with mocked invoke() in colocated test file
- Test file: `src-tauri/src/services/app_settings.rs`
- Test file: `src/pages/Settings.test.tsx`

## Change Log

| Date       | Version | Description                                                    | Author    |
| ---------- | ------- | -------------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                               | Romain G. |
| 2026-03-13 | 1.1     | Updated tech references for Rust/Tauri v2 + React/TS migration | Romain G. |
| 2026-03-20 | 1.2     | Added CredentialStore rule for secrets                         | Romain G. |
| 2026-03-25 | 2.0     | Removed language selector (moved to Epic 13), added Permissions tab | Romain G. |
