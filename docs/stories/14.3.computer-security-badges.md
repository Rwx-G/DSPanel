# Story 14.3: Computer Security Indicator Badges

## Status: Draft

## Story

**As an** AD operator,
**I want** visual badges on computer objects that flag dangerous Kerberos delegation configurations,
**so that** I can identify computers that are prime targets for Kerberos relay or constrained-delegation abuse before scheduling maintenance or decommissioning them.

## Acceptance Criteria

1. The `ComputerDetail` page (and equivalent area in `ComputerLookup`) renders a row of badges following the same pattern as Story 14.2.
2. Three computer-side indicators render their own badge: `UnconstrainedDelegation` (Critical - any compromise of this computer compromises every account that ever authenticated against it), `ConstrainedDelegation` (Warning, with the list of target SPNs in the tooltip), `RBCD` (Warning, with the list of allowed-to-act SIDs in the tooltip).
3. The tooltips list the actual values of `msDS-AllowedToDelegateTo` and `msDS-AllowedToActOnBehalfOfOtherIdentity` for the operator to verify what the configuration actually permits.
4. Translations are added to the relevant computer namespace in `src/locales/{en,fr,de,it,es}/`.
5. The computer lookup list mirrors the user-list dot pattern: highest severity dot, popover on hover listing all indicators.
6. Frontend unit tests cover the three indicator badges and the dot aggregation.

## Tasks / Subtasks

- [ ] Wire the Tauri command into the computer-detail data flow (AC: #1)
    - [ ] In `ComputerDetail.tsx` (or the equivalent computer detail view used by `ComputerLookup`), add a `useEffect` that calls `invoke<SecurityIndicatorSet>("evaluate_computer_security_indicators", { computer })` after the computer is loaded
    - [ ] Store the result in component state with the same loading pattern as the user side (Story 14.2)
- [ ] Render the badges (AC: #1, #2, #3)
    - [ ] Iterate `indicators` and render one `<StatusBadge>` per indicator with the variant from `indicator.severity`
    - [ ] For `ConstrainedDelegation`: tooltip body must include the actual list of target SPNs from `computer.rawAttributes["msDS-AllowedToDelegateTo"]`. The list goes in the tooltip body, not in the badge label.
    - [ ] For `RBCD`: tooltip body must include the actual list of SIDs from `computer.rawAttributes["msDS-AllowedToActOnBehalfOfOtherIdentity"]`. Note: this attribute holds a binary security descriptor blob, not a plain SID list - **the parsing happens in this story's frontend by walking the blob client-side, OR (preferred) by extending Story 14.1 backend to expose the parsed SID list as part of the `SecurityIndicator.metadata` field**. Make the choice explicit during implementation and document it in the Change Log of this story.
    - [ ] `data-testid="computer-security-indicator-badge-{kind}"`
- [ ] RBCD and ConstrainedDelegation tooltips read the `metadata` field already populated by Story 14.1 (AC: #3)
    - [ ] Story 14.1 was updated (post-PO review on 2026-04-26) to populate `SecurityIndicator.metadata = Some({ "allowed_principals": [...] })` for `Rbcd` and `metadata = Some({ "target_spns": [...] })` for `ConstrainedDelegation`. This story consumes those fields directly - no client-side SD parser is required.
    - [ ] In TypeScript, type the `metadata` field as `metadata?: { allowed_principals?: string[]; target_spns?: string[] }` (or `Record<string, unknown>` for forward compatibility); the renderer reads the relevant key based on `indicator.kind`.
- [ ] Reuse the SecurityIndicatorDot component built in Story 14.2 (AC: #5)
    - [ ] Confirm the component is already generic on indicators (it should be - it consumes `SecurityIndicatorSet`, not user-specific data)
    - [ ] Integrate the dot into `ComputerLookup.tsx`: batch-evaluate via Promise.all on the displayed page, cache in `Map<distinguishedName, SecurityIndicatorSet>`, render `<SecurityIndicatorDot indicators={...} />` next to the computer name in the row
- [ ] Add i18n translations (AC: #4)
    - [ ] For each of the 3 indicators, add `securityIndicators.{kind}.badge` and `securityIndicators.{kind}.tooltip` keys
    - [ ] Tooltip wording references the AD attribute, explains the attack vector in operator-friendly terms
    - [ ] If RBCD/ConstrainedDelegation tooltip needs to interpolate the principal list, use i18next interpolation: `t('securityIndicators.RBCD.tooltip', { principals: list.join(', ') })`
    - [ ] Translations in EN/FR/DE/IT/ES
- [ ] Frontend unit tests (AC: #6)
    - [ ] `ComputerDetail.test.tsx`: each of the 3 indicators renders its badge with the expected variant; tooltip for ConstrainedDelegation includes the SPN list; tooltip for RBCD includes the principal list
    - [ ] `ComputerLookup.test.tsx`: dot rendered for computers with indicators, hidden otherwise, color matches `highest_severity`
- [ ] Run `pnpm lint` + `pnpm exec tsc --noEmit` + `pnpm test src/pages/ComputerDetail.test.tsx src/pages/ComputerLookup.test.tsx` and confirm zero failures

## Dev Notes

### Architecture

Mirrors Story 14.2 on the computer side. The same `SecurityIndicator` / `SecurityIndicatorSet` types, the same `SecurityIndicatorDot` component, the same i18n key layout. Only the kinds differ.

### RBCD parsing - resolved during PO review

The attribute `msDS-AllowedToActOnBehalfOfOtherIdentity` is a binary security descriptor blob, not a plain SID list. The PO review on 2026-04-26 resolved the ambiguity: Story 14.1 was updated to expose the parsed principal SIDs via `SecurityIndicator.metadata = Some({ "allowed_principals": [...] })`, populated by the existing Rust SD parser in `services/dacl.rs`. This story therefore reads the field directly with no parsing logic on the frontend, and no TypeScript SD parser is needed.

The same pattern applies to `ConstrainedDelegation` whose `msDS-AllowedToDelegateTo` is a multi-valued string attribute (no parsing needed). The metadata field standardizes the contract with the UI: the renderer always reads `indicator.metadata`, irrespective of whether the underlying AD attribute is structured or plain.

### Reusing existing patterns

- `StatusBadge`, `SecurityIndicatorDot`, popover-on-hover - all from Story 14.2
- Batch evaluation pattern - from Story 14.2
- i18n namespace structure - same as Story 14.2

### i18n key structure

```json
{
  "securityIndicators": {
    "UnconstrainedDelegation": {
      "badge": "Unconstrained delegation",
      "tooltip": "This computer can impersonate any user that authenticates against it. A compromise of this host compromises every authenticated account. (userAccountControl bit TRUSTED_FOR_DELEGATION)"
    },
    "ConstrainedDelegation": {
      "badge": "Constrained delegation",
      "tooltip_with_targets": "This computer can impersonate users when authenticating to: {{targets}}. Verify each target SPN is still required. (msDS-AllowedToDelegateTo)"
    },
    "RBCD": {
      "badge": "RBCD",
      "tooltip_with_principals": "These principals can impersonate users to this computer: {{principals}}. (msDS-AllowedToActOnBehalfOfOtherIdentity)"
    }
  }
}
```

### Key file paths

- `src/pages/ComputerDetail.tsx` (updated - or wherever the detail view is rendered today)
- `src/pages/ComputerDetail.test.tsx` (updated)
- `src/pages/ComputerLookup.tsx` (updated)
- `src/pages/ComputerLookup.test.tsx` (updated)
- `src/locales/{en,fr,de,it,es}/computerDetail.json` (or new `securityIndicators.json` if shared with Story 14.2)
- (potentially) `src-tauri/src/services/security_indicators.rs` (amended to expose parsed SID lists in `metadata`)
- (potentially) `docs/stories/14.1.security-indicators-foundation.md` (Change Log appended for the metadata field)

### Severity rationale

- `UnconstrainedDelegation` = Critical: compromise of this single host compromises any account that ever authenticates against it. Equivalent to "Tier 0 by reachability".
- `ConstrainedDelegation` = Warning: more limited blast radius (only the listed target SPNs). Operator should verify the list is intentional and minimal.
- `RBCD` = Warning: the listed principals can impersonate users to this computer. Often legitimate (e.g. SQL Server delegation) but can be abused if a low-privilege account has write to `msDS-AllowedToActOnBehalfOfOtherIdentity`.

### Testing

- Same conventions as Story 14.2.
- Add a test fixture computer with all three indicators set (uac=0x80000, msDS-AllowedToDelegateTo=["http/web1.corp.local"], msDS-AllowedToActOnBehalfOfOtherIdentity=<binary SD blob bytes>) to verify that all three badges render simultaneously.

## Change Log

| Date       | Version | Description      | Author    |
| ---------- | ------- | ---------------- | --------- |
| 2026-04-26 | 1.0     | Initial creation | Romain G. |

## QA Results

(QA agent fills this section after the story is implemented.)
