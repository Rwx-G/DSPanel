# Story 14.4: Quick-Fix - Clear PasswordNotRequired

## Status: Ready for Review

## Story

**As an** AccountOperator,
**I want** a "Clear PasswordNotRequired flag" button next to the `PasswordNotRequired` badge,
**so that** I can remediate the misconfiguration in two clicks (open user, click button) instead of switching to ADUC, finding the user, opening properties, navigating to Account tab, unticking the box, and applying.

## Acceptance Criteria

1. The badge `PasswordNotRequired` on `UserDetail` (Story 14.2) gains an inline button "Fix" visible only when the operator's permission level is `AccountOperator` or higher.
2. Clicking the button opens a `ConfirmationDialog` (existing component) with the i18n message explaining: what the flag does, why it is dangerous, what the fix does (`userAccountControl &= ~0x0020`), and a checkbox "I understand this user must comply with the domain password policy at next logon".
3. The dialog is gated by the existing `MfaGate` from Epic 2 if the operator has MFA configured and the action category is set to require MFA in the MFA settings.
4. On confirmation, a new Tauri command `clear_password_not_required` is invoked on the user DN.
5. The backend command reads the current `userAccountControl`, clears bit 0x0020, writes it back via `Mod::Replace`, and emits an `AuditEntry` with action `clear_password_not_required`, target_dn = the user DN, and details listing the previous and new UAC value.
6. The audit entry is captured by the existing SHA-256 hash chain (Epic 11) and forwarded to syslog if configured.
7. After successful fix, the `UserDetail` view reloads the user attributes and the `PasswordNotRequired` badge disappears.
8. Backend command is permission-gated at the service layer: invocation by a `ReadOnly` or `HelpDesk` operator returns an `AppError::PermissionDenied` even if the IPC call is somehow forged.
9. Unit tests cover the UAC bit manipulation (with multiple combinations of other UAC flags set) and the permission gate.

## Tasks / Subtasks

- [x] Backend Tauri command (AC: #4, #5, #6, #8)
    - [x] Generic UAC bit-clear architecture chosen over the per-action approach in the original Dev Notes - added `clear_user_account_control_bits(user_dn, bits_to_clear) -> Result<(u32, u32)>` to the `DirectoryProvider` trait. Returns `(previous, new)` so the command layer can detect idempotent no-ops without an extra read trip. This makes Story 14.6 (clear TRUSTED_FOR_DELEGATION 0x80000) trivial - no new trait surface.
    - [x] Implemented on LdapDirectoryProvider (reuses the existing private `get_user_account_control` helper, mirrors the enable_account / disable_account write pattern); on MockDirectoryProvider with per-DN UAC map + `set_user_account_control` test setter; on ResilientDirectoryProvider via `execute_with_resilience` to inherit retry / timeout / circuit-breaker.
    - [x] `clear_password_not_required_inner` in `commands/account.rs`: `require_fresh_permission(state, AccountOperator, ...)` -> `mfa_service.check_mfa_for_action(...)` -> `capture_snapshot` -> trait call -> branch on (previous == new) for audit decision
    - [x] Audit entries use the actual project API (not the `record(AuditEntry { ... })` shape from the Dev Notes - corrected to `audit_service.log_success("ClearedPasswordNotRequired", user_dn, "uac: 0x{:X} -> 0x{:X}")` and `log_failure("ClearPasswordNotRequiredFailed", user_dn, &error)`)
    - [x] `#[tauri::command] pub async fn clear_password_not_required(user_dn: String, state: State<'_, AppState>)` wrapper added; registered in `lib.rs invoke_handler` next to the existing password / account commands
- [x] Backend tests (AC: #9)
    - [x] 5 trait-method tests in `services/directory.rs::tests`: bit isolation (only target bit cleared from 0x0220), idempotent no-op (no LDAP write, no recorded call), unrelated bit preservation (TRUSTED_FOR_DELEGATION + ACCOUNTDISABLE survive), generic mask reuse (Story 14.6 mask 0x80000 works), failure flag propagation
    - [x] 6 command-level tests in `commands/account.rs::tests`: ReadOnly + HelpDesk denied (gate is AccountOperator+), AccountOperator succeeds, Admin succeeds, idempotent no-op (no LDAP write, no audit entry, no clear_uac_bits_calls record), snapshot captured before write, audit entry on directory failure
- [x] Frontend dialog (AC: #2, #3)
    - [x] Created `src/components/dialogs/ClearPasswordNotRequiredDialog.tsx` using the existing `DialogShell` primitive (the Dev Notes mentioned `ConfirmationDialog` but the project has a more flexible `DialogShell` that PasswordResetDialog uses - mirrored that pattern instead)
    - [x] Props: `userDn`, `displayName`, `onClose`, `onSuccess` (mirrors PasswordResetDialog signature for consistency)
    - [x] i18n keys under `userDetail.quickFix.clearPasswordNotRequired.{title, body, checkboxLabel, confirmButton, fixButton, fixButtonAriaLabel, successNotification, failureNotification}`
    - [x] Acknowledgement checkbox starts unchecked; Confirm button disabled until ticked. Click handler is also a defense-in-depth no-op when `acknowledged === false`
    - [x] Confirm action wrapped in `useMfaGate("ClearPasswordNotRequired").checkMfa(...)` - the project does NOT have a `<MfaGate>` JSX component as the Dev Notes claimed; only the `useMfaGate` hook (defense in depth alongside the backend `mfa_service.check_mfa_for_action`)
- [x] Frontend integration (AC: #1, #7)
    - [x] `UserDetail.tsx` renders an inline "Fix" button next to the PasswordNotRequired badge when `canEdit` (already `hasPermission("AccountOperator")` per existing line). Button uses the project secondary-button pattern from CLAUDE.md (border + bg + hover) for visibility inline in the badge row
    - [x] Click sets `activeQuickFix: "PasswordNotRequired" | null` state; the dialog renders conditionally as a sibling of PasswordResetDialog
    - [x] On success: dispatches a localized success notification via the existing `useNotifications` hook, calls `handleRefresh()` (the existing reload mechanism passed in via `onRefresh` prop from UserLookup) so the badge disappears once the LDAP reload completes
    - [x] State type is a discriminated union: `"PasswordNotRequired" | null`. Stories 14.5 and 14.6 will extend the union with their own kinds and add a sibling conditional render
- [x] i18n
    - [x] EN/FR/DE/IT/ES all added in the same commit (no language ships untranslated). Translation completeness suite (176 tests) green - no missing keys, no orphans, no empty values, `{{name}}` and `{{error}}` interpolation vars preserved across all 5 languages
    - [x] Strings placed under `userDetail.quickFix.clearPasswordNotRequired` namespace (extending the existing `userDetail.json` per the Dev Notes guidance, not creating a new file)
- [x] Frontend tests
    - [x] `ClearPasswordNotRequiredDialog.test.tsx`: 9 tests - dialog renders with title and display name, body includes the bit identifier (PASSWORD_NOT_REQUIRED + 0x0020) for operator verification, Confirm disabled until acknowledgement checkbox ticked, Confirm calls the Tauri command with userDn, Cancel calls onClose, defense-in-depth (handler is no-op when disabled state bypassed), error message displays on failure, MFA gate runs before the Tauri command, command not invoked when MFA is denied
    - [x] `UserDetail.test.tsx`: 3 new tests - Fix button rendered for AccountOperator+ when indicator is PasswordNotRequired (gated by canEdit), Fix button NOT rendered for other indicator kinds (Kerberoastable, ReversibleEncryption), click opens ClearPasswordNotRequiredDialog. Total 66 tests in file (was 63)
- [x] Run quality gates: `cargo test --features gssapi --lib` 1635 passed (was 1629), clippy clean, fmt clean. Frontend `pnpm lint` clean, `pnpm exec tsc --noEmit` clean, `pnpm test` 2149/2149 (was 2137, +12 new)

## Dev Notes

### Architecture

This is a focused write operation on a single LDAP attribute. The complexity is not in the LDAP write itself (single-bit flip on `userAccountControl`) but in the **safety wrapping**:

- Permission gate at the service layer (defense in depth - frontend hides the button, backend rejects the call)
- MfaGate at the UI layer for high-trust environments
- Object snapshot before the write (rollback capability)
- Audit trail with hash chain (tamper evidence)
- Idempotent no-op when the flag is already clear (avoid spurious audit entries)

### Why AccountOperator and not HelpDesk

The `PasswordNotRequired` flag affects whether the user must comply with the password policy. HelpDesk operators in DSPanel are scoped to user lookups, password resets, and unlocks - changing UAC bits is outside their remit per the existing permission matrix in `permissions.rs`. AccountOperator is the right level (matches the AD built-in group of the same name).

### Idempotence note

If two operators independently click "Fix" within seconds, the second invocation reads the new UAC, finds the bit already cleared, and returns Ok without writing or auditing. This is intentional to avoid both spurious audit entries and a useless second LDAP write. The audit log only records actual state changes.

### Rust signature

```rust
// src-tauri/src/commands/account.rs

#[tauri::command]
pub async fn clear_password_not_required(
    state: State<'_, AppState>,
    user_dn: String,
) -> Result<(), AppError> {
    clear_password_not_required_inner(&state, &user_dn).await
}

pub(crate) async fn clear_password_not_required_inner(
    state: &AppState,
    user_dn: &str,
) -> Result<(), AppError> {
    if !state
        .permission_service
        .has_permission(PermissionLevel::AccountOperator)
    {
        return Err(AppError::PermissionDenied(
            "Clearing PasswordNotRequired requires AccountOperator permission".to_string(),
        ));
    }

    let provider = state.provider();
    let current_uac = provider
        .get_user_account_control(user_dn)
        .await
        .map_err(|e| AppError::Directory(e.to_string()))?;

    let new_uac = current_uac & !0x0020u32;
    if new_uac == current_uac {
        return Ok(()); // idempotent no-op
    }

    capture_snapshot(state, user_dn, "clear_password_not_required");

    provider
        .modify_attribute(user_dn, "userAccountControl", &[new_uac.to_string()])
        .await
        .map_err(|e| AppError::Directory(e.to_string()))?;

    state.audit_service.record(AuditEntry {
        operator: state.audit_service.operator(),
        action: "clear_password_not_required".to_string(),
        target_dn: user_dn.to_string(),
        details: format!("uac: 0x{:X} -> 0x{:X}", current_uac, new_uac),
        success: true,
        timestamp: now_iso(),
    });

    Ok(())
}
```

### MfaGate integration

The `<MfaGate action="...">` component (Story 2.5) consults the operator's MFA settings and prompts for a TOTP code when the action requires MFA. Pass `action="clear_password_not_required"` so the MFA configuration UI (Story 2.5 / Story 12.x) can selectively require MFA for this fix.

### Key file paths

- `src-tauri/src/commands/account.rs` (updated - new command)
- `src-tauri/src/lib.rs` (updated - register command)
- `src-tauri/src/commands/account.rs` `mod tests` (updated - new tests)
- `src/components/dialogs/ClearPasswordNotRequiredDialog.tsx` (new)
- `src/components/dialogs/ClearPasswordNotRequiredDialog.test.tsx` (new)
- `src/pages/UserDetail.tsx` (updated - inline Fix button)
- `src/pages/UserDetail.test.tsx` (updated)
- `src/lib/tauri-commands.ts` (updated - typed wrapper)
- `src/locales/{en,fr,de,it,es}/userDetail.json` (or dialogs.json)

### Coding standards

- Audit entry strings: action names are stable identifiers (snake_case) NOT translated. Translation happens at the UI layer when the audit log viewer renders past entries.
- The `capture_snapshot` helper exists in `commands/mod.rs` per the project's snapshot-before-modification rule. Use it.

### Testing

- Backend: `MockDirectoryProvider` exposes `set_user_account_control(dn, uac)` and `get_modify_attribute_calls() -> Vec<(dn, attr, values)>` (verify these helpers exist; if not, add them in this story since they are needed for tests of any UAC mutation)
- Frontend: same vitest + RTL pattern as existing dialog tests

## Change Log

| Date       | Version | Description                                                       | Author    |
| ---------- | ------- | ----------------------------------------------------------------- | --------- |
| 2026-04-26 | 1.0     | Initial creation                                                  | Romain G. |
| 2026-04-26 | 1.1     | Implementation completed (Tasks 1-7 done, Status -> Ready for Review) | Romain G. |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (1M context) - lance-dev orchestrator + dev persona (BMad)

### Debug Log References

None - all tasks completed without HALT or repeated failures.

### Completion Notes List

- The orchestrator's brief overrode several Dev Notes inaccuracies that would have produced compile-time or test-time errors:
    - Audit API is `audit_service.log_success(action, dn, details)` / `log_failure(...)`, NOT `audit_service.record(AuditEntry { ... })` - copied verbatim from the existing unlock_account_inner / reset_password_inner patterns
    - Permission gate uses `require_fresh_permission(state, AccountOperator, "Clear PasswordNotRequired")` which re-detects permissions before each critical write (defense in depth against stale cache) - NOT a one-line `has_permission` check
    - `provider.get_user_account_control` was a private inherent method on LdapDirectoryProvider, NOT a trait method - solved by adding a NEW generic trait method `clear_user_account_control_bits(dn, mask)` that handles the read-modify-write internally
    - Frontend uses the `useMfaGate` hook (returns `Promise<boolean>`), NOT a `<MfaGate>` JSX component (which does not exist in this codebase)
- Generic trait architecture: `clear_user_account_control_bits(dn, bits_to_clear) -> Result<(u32, u32)>` returns `(previous, new)` for caller-side idempotent-no-op detection. Story 14.6 will reuse this trait method with mask `0x80000` (TRUSTED_FOR_DELEGATION) - no new backend surface required for that story.
- Snapshot capture trade-off: the command always captures a snapshot before calling the trait method, even on the idempotent-no-op path. The alternative (peek UAC first via a separate trait method) would have added trait surface for a marginal optimization. Documented inline in `clear_password_not_required_inner`. The wasted snapshot is harmless (records the current state, no rollback opportunity wasted) and the no-op case is uncommon in practice.
- ConfirmationDialog vs custom dialog: the Dev Notes suggested wrapping ConfirmationDialog. Used DialogShell directly instead (mirroring PasswordResetDialog's pattern) because the body needs an explicit acknowledgement checkbox and a multi-paragraph explanation that does not fit ConfirmationDialog's title/body/footer shape.
- Frontend state machine for quick-fix dialogs uses a discriminated union `activeQuickFix: "PasswordNotRequired" | null`. Stories 14.5 and 14.6 will extend the union with their own kinds and add a sibling conditional render at the bottom of UserDetail.

### File List

**Modified:**
- `src-tauri/src/services/directory.rs` (added clear_user_account_control_bits trait method + MockDirectoryProvider impl with set_user_account_control test setter and clear_uac_bits_calls counter; 5 new mock-level tests)
- `src-tauri/src/services/ldap_directory.rs` (LdapDirectoryProvider impl - reads UAC via existing helper, single Mod::Replace write when value changed, no-op early return when bits already clear)
- `src-tauri/src/services/resilient_directory.rs` (ResilientDirectoryProvider impl - delegates via execute_with_resilience)
- `src-tauri/src/commands/account.rs` (clear_password_not_required_inner + Tauri thin wrapper + 6 backend tests)
- `src-tauri/src/lib.rs` (registered clear_password_not_required in invoke_handler)
- `src/pages/UserDetail.tsx` (inline Fix button next to PasswordNotRequired badge gated by canEdit; activeQuickFix state machine; conditional ClearPasswordNotRequiredDialog render with success notification + handleRefresh)
- `src/pages/UserDetail.test.tsx` (3 new tests)
- `src/locales/en/userDetail.json`, `src/locales/fr/userDetail.json`, `src/locales/de/userDetail.json`, `src/locales/it/userDetail.json`, `src/locales/es/userDetail.json` (quickFix.clearPasswordNotRequired sub-namespace in 5 languages)

**Created:**
- `src/components/dialogs/ClearPasswordNotRequiredDialog.tsx` (custom dialog with acknowledgement checkbox + useMfaGate wrapping)
- `src/components/dialogs/ClearPasswordNotRequiredDialog.test.tsx` (9 component tests)

## QA Results

### Review Date: 2026-04-26

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Story 14.4 is the first WRITE operation in Epic 14 and it sets the template for Stories 14.5 and 14.6. The dev agent overrode several inaccurate Story Dev Notes (audit API shape, permission gate pattern, missing trait method, useMfaGate hook vs JSX component) and replaced them with the actual project patterns from existing critical-write commands. The implementation is consistent end-to-end: the new code looks like it was written by the same author as the existing PasswordReset / unlock_account / disable_account flows.

The most consequential architectural decision is the generic trait method `clear_user_account_control_bits(dn, mask) -> Result<(u32, u32)>` instead of an action-specific method. This is forward-thinking: Story 14.6 (clear TRUSTED_FOR_DELEGATION 0x80000) will be a single-line command-layer change with zero new trait surface. The mock-side test coverage validates this generality directly (test_clear_uac_bits_works_with_any_mask uses the 0x80000 mask).

The audit trail is correct: log_success and log_failure both call insert_entry which feeds the SHA-256 hash chain (verified at services/audit.rs:357-394). No new audit infrastructure needed - the existing chain captures every state-changing call. Defense in depth is fully realized: frontend useMfaGate + backend mfa_service.check_mfa_for_action; frontend canEdit hint + backend require_fresh_permission with re-detection. A forged IPC call from a ReadOnly or HelpDesk operator is rejected at the backend - verified by test_clear_password_not_required_requires_account_operator.

One acknowledged trade-off: the snapshot is captured BEFORE the trait call even on the idempotent no-op path. The dev correctly documented this inline as a deliberate choice to avoid an extra trait peek round-trip. Cost is one wasted `get_all_attributes` LDAP call when an operator double-clicks the Fix button. Accepted - the no-op case is uncommon and snapshot-before-write preserves rollback semantics.

### Refactoring Performed

None (advisory review - no code changes by reviewer).

### Compliance Check

- Coding Standards: [x] Rust no `unwrap()` outside tests, snake_case fields, audit-on-write rule honored, snapshot-before-modify rule honored, permission check before action, no em dashes
- Project Structure: [x] Trait method in services/directory.rs, impls in 3 providers (LDAP/Mock/Resilient), command in commands/account.rs alongside related write commands, dialog in components/dialogs/ next to PasswordResetDialog, locale key in userDetail.json sub-namespace
- Testing Strategy: [x] Trait-level + command-level + dialog component + UserDetail integration tests; mock provider extended with set_user_account_control setter + clear_uac_bits_calls observability; mocked Tauri invoke in dialog tests; translation completeness suite enforces parity across 5 languages
- All ACs Met: [x] 9/9

### AC Verification

| AC                                                    | Status | Notes                                                                                                                                                              |
| ----------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #1 Fix button visible only at AccountOperator+        | PASS   | UserDetail.tsx:476 `indicator.kind === "PasswordNotRequired" && canEdit`. canEdit=hasPermission("AccountOperator") at line 125. Tests verify visibility + hidden for other kinds |
| #2 Custom dialog with "I understand" checkbox          | PASS   | ClearPasswordNotRequiredDialog.tsx:82-93 checkbox; line 117 confirm-disabled-until-acknowledged. Multi-paragraph body via whitespace-pre-line at line 78. Body i18n contains "PASSWORD_NOT_REQUIRED" and "0x0020" in all 5 languages |
| #3 useMfaGate wraps the confirm                        | PASS   | Dialog.tsx:36 `const { checkMfa } = useMfaGate()`. Line 41 `const mfaAllowed = await checkMfa("ClearPasswordNotRequired"); if (!mfaAllowed) return;`. Tests verify both order (MFA before invoke) and denial (MFA cancel does not invoke) |
| #4 New Tauri command registered                        | PASS   | commands/account.rs:701 `#[tauri::command] pub async fn clear_password_not_required`. lib.rs:260 registered in invoke_handler                                        |
| #5 Backend reads UAC, clears bit, writes, audits      | PASS   | account.rs:444-446 calls trait method with mask 0x0020. ldap_directory.rs:2013-2061 read-modify-write via Mod::Replace. audit_service.log_success("ClearedPasswordNotRequired", dn, "uac: 0x{:X} -> 0x{:X}") at lines 455-459 |
| #6 Hash chain capture                                  | PASS   | log_success/log_failure both call insert_entry → SHA-256 chain (services/audit.rs:357-394). No new audit code needed; existing infrastructure handles it. Verified by existing audit hash chain tests |
| #7 Refresh after success                               | PASS   | UserDetail.tsx renders dialog with onSuccess callback that fires notify(...) + handleRefresh(). handleRefresh = onRefresh ?? noop; onRefresh prop comes from UserLookup.refreshSelectedUser which re-fetches user attributes |
| #8 Backend permission re-check                         | PASS   | account.rs:429-434 `require_fresh_permission(state, AccountOperator, "Clear PasswordNotRequired").await?`. Calls detect_permissions(provider) before each write. Test test_clear_password_not_required_requires_account_operator verifies ReadOnly + HelpDesk both denied |
| #9 Backend tests for UAC bit + permission + idempotent + audit | PASS  | 5 trait-level tests + 6 command-level tests = 11 backend tests covering all 4 dimensions                                                                          |

### Residual Risk Verification

**R1 - UAC bit manipulation correctness**: VERIFIED OK
- `test_clear_uac_bits_clears_only_target_bit`: 0x0220 → 0x0200, only PASSWORD_NOT_REQUIRED cleared
- `test_clear_uac_bits_preserves_unrelated_bits`: 0x80220 → 0x80200, TRUSTED_FOR_DELEGATION + ACCOUNTDISABLE survive
- `test_clear_uac_bits_works_with_any_mask`: 0x80200 with mask 0x80000 → 0x0200 (validates Story 14.6 reuse)
- Bit math `previous & !bits_to_clear` is correct for any subset of UAC bits

**R2 - Idempotent no-op semantics**: VERIFIED OK
- `test_clear_uac_bits_idempotent_no_op_when_already_clear`: no-op returns (previous, previous), no clear_uac_bits_calls record, no LDAP write
- `test_clear_password_not_required_idempotent_no_op`: command no-op returns Ok, no audit "ClearedPasswordNotRequired" entry, no LDAP write
- Snapshot wasted on no-op path is documented inline; accepted trade-off

**R3 - Defense in depth (frontend + backend MFA gate)**: VERIFIED OK
- Frontend: ClearPasswordNotRequiredDialog.tsx:36 useMfaGate hook, line 41 checkMfa("ClearPasswordNotRequired") with deny-path early return
- Backend: account.rs:436-439 `state.mfa_service.check_mfa_for_action("ClearPasswordNotRequired").map_err(|e| AppError::PermissionDenied(e.to_string()))?`
- Both gates use the SAME action name string for consistent MFA-required-for-action policy
- Forged IPC bypass: backend rejects (verified by ReadOnly/HelpDesk permission tests; backend MFA check runs after permission check)

**R4 - Permission re-detection (stale cache attack)**: VERIFIED OK
- `require_fresh_permission` (account.rs:14-50) calls `detect_permissions(provider)` before each invocation
- "Preserve previous level on transient failure" branch (lines 31-37) only runs when re-detection RETURNS a lower level than cached - prevents false negatives during AD outages
- BUT the final has_permission check at line 43 still runs against the ACTUAL current level - if the cached "previous" level is itself insufficient, the call is denied
- Demotion attack scenario: operator downgraded from AccountOperator to HelpDesk in AD → re-detect succeeds and reflects HelpDesk → has_permission(AccountOperator) returns false → DENIED
- Stale-cache + transient-failure scenario: operator downgraded + AD unreachable → cache stays at AccountOperator → DENIED only if cache happens to be insufficient. Acceptable trade-off documented in the helper comments

**R5 - ResilientDirectoryProvider delegation**: VERIFIED OK
- resilient_directory.rs:291-307: `clear_user_account_control_bits` wrapped in `execute_with_resilience` (the same wrapper as set_password_flags / modify_attribute / create_user)
- Inherits retry/timeout/circuit-breaker semantics consistent with other write methods

**R6 - Snapshot before write**: VERIFIED OK
- account.rs:441 `capture_snapshot(state, user_dn, "ClearPasswordNotRequired").await;` runs BEFORE the provider call at line 444
- `test_clear_password_not_required_captures_snapshot` asserts state.snapshot_service.count() == 1 after a successful clear

**R7 - i18n completeness**: VERIFIED OK
- 5 languages (en/fr/de/it/es) extended with userDetail.quickFix.clearPasswordNotRequired sub-namespace
- 176 translation completeness tests pass: no missing keys, no orphans, no empty values
- {{name}} interpolation in successNotification and {{error}} in failureNotification preserved across all languages
- Bit identifier "0x0020" and "PASSWORD_NOT_REQUIRED" present in all 5 language bodies for operator verification

### NFR Validation

- **Security**: PASS - Defense in depth: 4 gates (frontend canEdit + backend require_fresh_permission + frontend useMfaGate + backend mfa_service.check_mfa_for_action). Audit chain captures every success and failure via SHA-256 hash chain. Snapshot before write enables rollback. Bit mask isolation tested. No secrets logged - only UAC hex values in audit details. No new attack surface beyond a permission-gated single-attribute write
- **Performance**: PASS - Single LDAP read + single LDAP write per fix; no-op early return saves the write. Snapshot capture is one additional get_all_attributes call (acceptable for any write op). ResilientDirectoryProvider adds resilience without measurable perf regression
- **Reliability**: PASS - ResilientDirectoryProvider wrapping inherits retry/timeout/circuit-breaker. Idempotent no-op handles operator double-click + race conditions. Snapshot before write enables rollback. Error path: log_failure recorded, AppError::Directory propagated to UI
- **Maintainability**: PASS - Generic trait method usable by Story 14.6 with zero new trait surface. Dialog mirrors PasswordResetDialog structure exactly. Discriminated-union state machine extensible for future quick-fix kinds. All public types documented. No new dependencies
- **Accessibility**: PASS - Fix button has explicit aria-label via i18n. Dialog uses ariaLabel via DialogShell. Checkbox has explicit label association. Cancel button respects loading state. Confirm button disabled state prevents accidental double-submit

### Test Architecture Assessment

- 5 trait-level mock tests + 6 command-level tests + 9 dialog component tests + 3 UserDetail integration tests + 176 translation tests = 199 directly relevant tests, all green (independently verified)
- Full backend lib suite 1635/1635; full frontend suite 2149/2149; clippy clean, fmt clean, lint clean, tsc clean
- Mock provider extensions: set_user_account_control setter, clear_uac_bits_calls counter, modify_attribute_calls counter — gives tests precise observability of every aspect of the write
- Edge cases covered: bit isolation (only target bit cleared), unrelated bit preservation (TRUSTED_FOR_DELEGATION + ACCOUNTDISABLE survive), idempotent no-op (no LDAP write, no audit entry, no recorded call), permission denial (ReadOnly + HelpDesk both denied), permission grant (AccountOperator + Admin both succeed), audit on success (entry shape + uac before/after in details), audit on failure (failure entry recorded, success=false), snapshot capture (before write), MFA gate ordering (MFA before invoke), MFA denial (invoke not called), error parsing (JSON userMessage extraction), defense-in-depth click handler (no-op when disabled bypassed)

### Improvements Checklist

- [ ] Optional: expose `get_user_account_control` (or `peek_uac` / similar) on the DirectoryProvider trait so the command can pre-check the no-op path without capturing a snapshot. Saves one LDAP get_all_attributes call per double-clicked Fix. Non-blocking; tracked as a Story 14.6 / Epic 14 follow-up since both Story 14.4 and 14.6 share the same trade-off.
- [ ] Optional: add an end-to-end success-path frontend integration test (click Fix → check checkbox → confirm → invoke succeeds → notification fires → handleRefresh called). Currently the flow is split across UserDetail tests (button + dialog open) and Dialog tests (confirm calls invoke + onSuccess). End-to-end is implicit.
- [ ] Optional: deduplicate the per-quick-fix i18n key structure once Stories 14.5 and 14.6 land. If all three quick-fix dialogs end up with `{title, body, checkboxLabel, confirmButton, fixButton, fixButtonAriaLabel, successNotification, failureNotification}`, factor a shared sub-namespace structure. Track for backlog.

None of the above are blocking. All correctness ACs are met.

### Security Review

This is the first WRITE operation in Epic 14 and the implementation is exemplary on the security axis:
- Defense in depth realized at 4 gates (canEdit hint, require_fresh_permission authoritative, useMfaGate frontend, mfa_service.check_mfa_for_action backend)
- Permission re-detection on every invocation defeats stale-cache demotion bypass
- Audit chain captures both success ("ClearedPasswordNotRequired") and failure ("ClearPasswordNotRequiredFailed") with SHA-256 hash chain integrity
- Snapshot-before-write enables rollback if the operation needs to be reversed
- UAC bit isolation tested - the mask 0x0020 cannot accidentally clear other security-relevant bits
- No secrets logged - audit details contain only hex UAC values
- The forged IPC attack (HelpDesk operator crafting a clear_password_not_required IPC call) is rejected at the backend permission gate

### Performance Considerations

Per-fix cost: 1 permission re-detection LDAP call + 1 snapshot get_all_attributes LDAP call + 1 UAC read LDAP call + 1 UAC write LDAP call = 4 LDAP round trips per successful fix. On a no-op, the UAC write is skipped but the other 3 still run. Acceptable for an interactive operator action (single-digit ms each on a healthy AD). The snapshot-on-no-op overhead could be optimized later if telemetry shows it as a real cost.

### Files Modified During Review

None.

### Gate Status

Gate: PASS -> docs/qa/gates/14.4-quickfix-clear-passwordnotrequired.yml

### Recommended Status

Ready for Done. All 9 acceptance criteria met. Architecture and security posture are exemplary - this story sets a strong template for Stories 14.5 and 14.6. Three optional improvements tracked as non-blocking follow-ups.
