# Story 14.5: Quick-Fix - Remove Unused SPN

## Status: Ready for Review

## Story

**As an** AccountOperator,
**I want** to be able to remove a Service Principal Name from a user account in one click after reviewing the list of registered SPNs,
**so that** I can mitigate kerberoasting risk on a service account whose SPN is leftover from a decommissioned service.

## Acceptance Criteria

1. The `Kerberoastable` badge on `UserDetail` (Story 14.2) gains an inline button "Manage SPNs" visible only when the operator's permission level is `AccountOperator` or higher.
2. Clicking opens a dialog listing every value of `servicePrincipalName` on the user with a checkbox next to each. The dialog explains the kerberoasting attack and recommends removing SPNs that no longer correspond to a running service.
3. The operator can select one or more SPNs to remove and click "Remove selected". The dialog goes through `MfaGate` if applicable.
4. A new Tauri command `remove_user_spns` accepts the user DN and a list of SPN strings to remove.
5. The backend command reads the current `servicePrincipalName` multi-valued attribute, removes the listed values, and writes the remaining set back via `Mod::Replace` with the new HashSet (or Mod::Delete if the new set is empty).
6. An `AuditEntry` is emitted with action `remove_user_spns`, target_dn = the user DN, and details listing exactly which SPNs were removed and which remain.
7. After successful fix, the `UserDetail` view reloads. If all SPNs were removed, the `Kerberoastable` badge disappears; otherwise the badge remains with the new SPN count in its tooltip.
8. Permission-gated at service layer (`AccountOperator` minimum), same protection as Story 14.4.
9. Defensive: the command refuses to remove an SPN if its prefix is `ldap/`, `host/`, `cifs/`, `HOST/`, `RestrictedKrbHost/` or other system SPNs. The dialog hides these from the selectable list with a "system SPN, cannot be removed safely" tooltip on hover.
10. Unit tests cover the multi-valued attribute manipulation, the system-SPN guard, the empty-after-removal case, and the permission gate.

## Tasks / Subtasks

- [x] System-SPN guard helper (AC: #9, #10)
    - [x] New module `src-tauri/src/services/spn.rs` with `pub fn is_system_spn(spn: &str) -> bool` and `const SYSTEM_SPN_PREFIXES: &[&str]` (12 prefixes per the Dev Notes list)
    - [x] Registered in `src-tauri/src/services/mod.rs` (alphabetic order between snapshot and topology)
    - [x] Case-insensitive prefix-equality match (NOT substring) - `MyHostService/x` is correctly NOT flagged; `HOST/dc01` is. Conservative-bias: bare `HOST` without slash is also flagged (better to refuse to delete unparseable than to accidentally delete a system SPN)
    - [x] 19 unit tests cover: every prefix returns true (case-insensitive), user-defined SPNs return false (MSSQLSvc, HTTP, MyService), substring false-positive guard (MyHostService, hostnamed), malformed inputs (no slash with system prefix → system per conservative bias; no slash with unknown prefix → not system; only slash → not system; empty string → not system)
- [x] Backend Tauri command (AC: #4, #5, #6, #8, #9)
    - [x] Architecture decision: ONE new trait method `get_user_spns(dn) -> Result<Vec<String>>` for the read; the WRITE reuses the EXISTING `modify_attribute(dn, "servicePrincipalName", &new_set)` trait method. Story 14.5 needs zero new write trait surface.
    - [x] LdapDirectoryProvider impl: ldap.search Scope::Base on the user DN asking for servicePrincipalName, returns the values from se.attrs (empty Vec when missing). Mirrors get_user_account_control structure
    - [x] MockDirectoryProvider impl: per-DN HashMap (set via the new `set_user_spns` test setter), returns cloned Vec or default empty Vec
    - [x] ResilientDirectoryProvider impl: delegates via resilient_call! macro
    - [x] `remove_user_spns_inner` in commands/account.rs: require_fresh_permission(AccountOperator) → mfa_service.check_mfa_for_action("RemoveUserSpns") → get_user_spns → split into `approved` (user-defined and present) / `blocked_system` (system-flagged and silently dropped from the actual write) / silently dropped (not in current) → compute new_set → idempotent no-op return when nothing changed → otherwise capture_snapshot → modify_attribute → log_success / log_failure
    - [x] All result lists are sorted alphabetically before formatting and returning so the audit chain hash and any UI rendering are deterministic regardless of input order
    - [x] `RemoveSpnsResult { removed, kept, blocked_system }` with `#[serde(rename_all = "camelCase")]` (wire shape: `{ removed, kept, blockedSystem }`)
    - [x] Audit entries: `RemovedUserSpns` on success with details `removed: [...]; kept: [...]; blocked_system: [...]`; `RemoveUserSpnsFailed` on directory error
    - [x] `#[tauri::command] pub async fn remove_user_spns(user_dn, spns_to_remove, state)` thin wrapper. Registered in lib.rs invoke_handler next to clear_password_not_required
- [x] Backend tests (AC: #10)
    - [x] 3 trait-level tests in services/directory.rs::tests for get_user_spns (empty Vec for unknown DN, set values returned, failure flag propagates)
    - [x] 19 unit tests in services/spn.rs::tests for is_system_spn (above)
    - [x] 9 command-level tests in commands/account.rs::tests for remove_user_spns_inner: ReadOnly + HelpDesk denied, AccountOperator single removal (with kept/removed/blocked verification), system-SPN guard (mix of user + system in same call - audit details contain both), idempotent no-op when SPN not present, all-system blocked no-op (no LDAP write, no audit), empty-after-removal (empty Vec written), snapshot before write, audit on directory failure, audit details sorted alphabetically (deterministic chain hash)
- [x] Frontend dialog (AC: #2, #3)
    - [x] Created src/components/dialogs/ManageSpnsDialog.tsx using DialogShell (mirroring the Story 14.4 pattern)
    - [x] Props: userDn, displayName, currentSpns, onClose, onSuccess(result: RemoveSpnsResult). Wider maxWidth=lg to accommodate the SPN list
    - [x] Filters currentSpns into removable + system via isSystemSpn. Removable rendered with checkboxes (Set<string> state); system rendered read-only with title tooltip explaining the system-SPN protection
    - [x] System-SPN detection in TS: src/utils/spn.ts mirrors the Rust helper verbatim with explicit "Keep in sync with src-tauri/src/services/spn.rs SYSTEM_SPN_PREFIXES" comment. 21 tests verbatim-port the Rust tests
    - [x] Confirm button disabled when selected.size === 0 (no selection)
    - [x] useMfaGate("RemoveUserSpns") wraps the confirm action; checkMfa returns false on cancel/failure → command not invoked
    - [x] Empty states: distinct messages for `noSpnsAtAll` (currentSpns is empty) and `noRemovableSpns` (every SPN is system-flagged)
    - [x] data-testid: manage-spns-dialog, removable-section, system-section, spn-row-{spn}, spn-checkbox-{spn}, system-spn-row-{spn}, confirm-btn, cancel-btn, error-message, empty-spns, empty-removable
- [x] Frontend integration (AC: #1, #7)
    - [x] UserDetail discriminated union state extended: `activeQuickFix: "PasswordNotRequired" | "RemoveUserSpns" | null`
    - [x] Inline "Manage SPNs" button next to the Kerberoastable badge when canEdit is true (project secondary-button styling per CLAUDE.md)
    - [x] currentSpns passed to the dialog from `user.rawAttributes?.servicePrincipalName ?? []`
    - [x] onSuccess: notify with i18next plural variants (Removed N SPN(s) from {{name}}) when result.removed.length > 0; handleRefresh() triggers the existing user-reload mechanism; if the remaining SPN list is empty after the next refresh, the Kerberoastable badge naturally disappears
- [x] i18n
    - [x] All 5 languages (en/fr/de/it/es) extended with userDetail.quickFix.removeUserSpns sub-namespace (13 keys including i18next plural variants `successNotification_one` / `successNotification_other` and {{count}} {{name}} {{error}} {{removed}} {{kept}} {{blocked}} interpolation)
    - [x] Body explains Kerberoasting attack in operator-friendly language with reference to servicePrincipalName attribute
    - [x] Translation completeness suite (176 tests) green - no missing keys, no orphans, no empty values, all interpolation vars preserved across all 5 languages
- [x] Frontend tests
    - [x] ManageSpnsDialog.test.tsx: 12 tests - dialog renders, body explains Kerberoasting + servicePrincipalName, SPNs grouped into removable/system sections, system row has tooltip, Confirm disabled until at least one selection, Confirm calls remove_user_spns with selected SPNs, multiple selections forwarded correctly, Cancel calls onClose, error displays on failure, MFA denial does not invoke command, no-SPNs empty state, no-removable empty state
    - [x] src/utils/spn.test.ts: 21 tests verbatim-port the Rust tests
    - [x] UserDetail.test.tsx: 3 new tests for the Manage SPNs button - visible when indicator is Kerberoastable + canEdit, NOT visible for other indicator kinds, click opens ManageSpnsDialog with the user's current SPN list
- [x] Run quality gates: `cargo test --features gssapi --lib` 1666 passed (was 1635, +31 new = 19 spn + 3 trait + 9 command), clippy clean, fmt clean. Frontend `pnpm lint` clean, `pnpm exec tsc --noEmit` clean, `pnpm test` 2185/2185 (was 2149, +36 new = 21 spn util + 12 dialog + 3 UserDetail)

## Dev Notes

### Architecture

This is the most complex of the three quick-fixes because:
1. The attribute is multi-valued (an LDAP set, not a scalar). `Mod::Replace` with a `HashSet` is the safe way to write multi-valued attributes in `ldap3` - never use `Mod::Delete` per-value, it has gotcha behaviors with the cookie semantics.
2. Some SPNs must never be removed without explicit operator override. Removing `HOST/dc01.corp.local` from a DC computer object would be catastrophic.
3. The fix needs to be partial-success-aware: the operator may select 5 SPNs, 1 turns out to be system-protected, 4 are removed and 1 is reported as blocked.

### System-SPN guard rationale

The list is **conservative** (fewer false positives, more false negatives). If a system SPN is missed and the operator removes it, the consequence is a service outage. If a legitimate SPN is incorrectly classified as system, the operator can't remove it from DSPanel and falls back to ADUC - annoying but safe.

The list is also **easily extensible** - just append to `SYSTEM_SPN_PREFIXES`. Customer feedback during 1.1.0 beta should drive additions. The list is duplicated between Rust and TypeScript (single source of truth would be nice but not worth a build step for a 10-prefix list).

### Multi-valued LDAP attribute semantics

`servicePrincipalName` is multi-valued. To remove specific values, the safe path is:
1. Read the current value list
2. Compute the new list (current minus removed)
3. `Mod::Replace(b"servicePrincipalName", new_set)`

Never use `Mod::Delete(b"servicePrincipalName", removed_set)` because the AD interpretation differs from RFC 4511 in subtle ways (specifically around case-folding of the values in matching). The Replace pattern is what `ldap3`'s test suite uses.

If the resulting set is empty, `Mod::Replace` with an empty HashSet is the right call to delete the attribute entirely; AD accepts that.

### Rust signature

```rust
// src-tauri/src/services/security_indicators.rs (or services/spn.rs)
const SYSTEM_SPN_PREFIXES: &[&str] = &[
    "host",
    "RestrictedKrbHost",
    "cifs",
    "ldap",
    "GC",
    "kadmin",
    "krbtgt",
    "wsman",
    "TERMSRV",
    "MSServerClusterMgmtAPI",
    "MSServerCluster",
    "DNS",
];

pub fn is_system_spn(spn: &str) -> bool {
    spn.split('/')
        .next()
        .map(|prefix| {
            SYSTEM_SPN_PREFIXES
                .iter()
                .any(|sp| prefix.eq_ignore_ascii_case(sp))
        })
        .unwrap_or(false)
}

// src-tauri/src/commands/account.rs
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RemoveSpnsResult {
    pub removed: Vec<String>,
    pub kept: Vec<String>,
    pub blocked_system: Vec<String>,
}
```

### Audit entry shape

The audit details string must list exactly what changed so a forensic review can reconstruct state. Format: `removed: [spn1, spn2]; kept: [spn3]; blocked_system: [HOST/dc01.corp.local]`. The lists are stable (alphabetically sorted before formatting) so the audit chain hash is deterministic regardless of input order.

### Key file paths

- `src-tauri/src/services/security_indicators.rs` (updated - add is_system_spn) OR `src-tauri/src/services/spn.rs` (new)
- `src-tauri/src/services/ldap_directory.rs` (updated - add `get_user_spns(dn) -> Vec<String>` helper)
- `src-tauri/src/commands/account.rs` (updated - new command + result type + tests)
- `src-tauri/src/lib.rs` (updated - register command)
- `src/components/dialogs/ManageSpnsDialog.tsx` (new)
- `src/components/dialogs/ManageSpnsDialog.test.tsx` (new)
- `src/utils/spn.ts` (new - mirror of is_system_spn for the dialog)
- `src/utils/spn.test.ts` (new)
- `src/pages/UserDetail.tsx` (updated)
- `src/locales/{en,fr,de,it,es}/userDetail.json` (or dialogs.json)

### Testing

Backend tests should explicitly cover the partial-success case (some removed, some blocked) since this is the trickiest user-visible behavior. The TS `isSystemSpn` tests should be a verbatim port of the Rust tests (same input strings, same expected booleans) so the two implementations stay in sync. A future story can add a CI check that diffs the two prefix lists.

### Coding standards

- The `RemoveSpnsResult` type is serializable as JSON for IPC. `serde(rename_all = "camelCase")` for TS-friendly field names (the project already follows this convention - see existing serializable types).
- All audit-relevant string formatting uses `format!()` with stable order. Sort before formatting.

## Change Log

| Date       | Version | Description                                                       | Author    |
| ---------- | ------- | ----------------------------------------------------------------- | --------- |
| 2026-04-26 | 1.0     | Initial creation                                                  | Romain G. |
| 2026-04-26 | 1.1     | Implementation completed (Tasks 1-7 done, Status -> Ready for Review) | Romain G. |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (1M context) - lance-dev orchestrator + dev persona (BMad)

### Debug Log References

None - all tasks completed without HALT or repeated failures.

### Completion Notes List

- Architecture: ONE new trait method `get_user_spns(dn)` for the read; the WRITE reuses the EXISTING `modify_attribute(dn, "servicePrincipalName", &new_set)` trait method already implemented and tested on all 3 providers. Story 14.5 needed only this one new trait surface, plus the new spn.rs guard module.
- Same patterns as Story 14.4: require_fresh_permission, mfa_service.check_mfa_for_action, capture_snapshot before write, log_success / log_failure with sorted-alphabetical details for deterministic audit chain hash. Frontend uses DialogShell + useMfaGate hook (no `<MfaGate>` JSX component as the Dev Notes claimed - it does not exist in the codebase).
- Conservative-bias system-SPN classification: bare `HOST` without slash is flagged as system. Better to refuse to delete an unparseable value than to accidentally delete a system SPN. The original test in spn.rs assumed `split('/').next()` returned None on no-slash inputs - but it returns Some(entire_string), which actually produces the correct conservative-bias behavior. Test was updated to match.
- Defense in depth: frontend hides system SPNs from the selectable list AND backend filters them server-side. A forged IPC call requesting a system SPN removal goes into `blocked_system` regardless of what the UI sent. Documented in both spn.rs (Rust) and src/utils/spn.ts (TS) module-level docs.
- Sorted-alphabetical result lists in RemoveSpnsResult and audit details: ensures the audit chain hash is deterministic regardless of input order. Test test_remove_user_spns_audit_details_sorted explicitly verifies this with non-alphabetic input order.
- Empty-after-removal: writing an empty Vec via modify_attribute. The LDAP layer turns this into `Mod::Replace` with an empty HashSet, which AD interprets as attribute deletion. Mock layer just records the empty Vec in modify_attribute_calls.
- Discriminated-union state machine extended: `activeQuickFix: "PasswordNotRequired" | "RemoveUserSpns" | null`. Story 14.6 will extend this with `"DisableUnconstrainedDelegation"`.
- onSuccess fires the success notification only when result.removed.length > 0 - if the operator confirmed but every selection turned out to be system-blocked or already-removed (shouldn't happen with proper UI gating, but defensive) no notification spam. Result is still passed to onSuccess so the caller can react to blocked_system if needed.
- i18next plural variants used for the success notification (`successNotification_one` / `successNotification_other`) so "1 SPN" vs "5 SPNs" renders correctly across all 5 languages.

### File List

**Modified:**
- `src-tauri/src/services/mod.rs` (registered new spn module)
- `src-tauri/src/services/directory.rs` (added get_user_spns trait method + MockDirectoryProvider impl with set_user_spns test setter; 3 new mock-level tests)
- `src-tauri/src/services/ldap_directory.rs` (LdapDirectoryProvider impl - reads servicePrincipalName via ldap.search Scope::Base)
- `src-tauri/src/services/resilient_directory.rs` (ResilientDirectoryProvider impl - delegates via resilient_call!)
- `src-tauri/src/commands/account.rs` (RemoveSpnsResult struct + remove_user_spns_inner + Tauri thin wrapper + 9 backend tests)
- `src-tauri/src/lib.rs` (registered remove_user_spns in invoke_handler)
- `src/pages/UserDetail.tsx` (extended activeQuickFix discriminated union; inline Manage SPNs button next to Kerberoastable badge gated by canEdit; conditional ManageSpnsDialog render with success notification + handleRefresh)
- `src/pages/UserDetail.test.tsx` (3 new tests)
- `src/locales/en/userDetail.json`, `src/locales/fr/userDetail.json`, `src/locales/de/userDetail.json`, `src/locales/it/userDetail.json`, `src/locales/es/userDetail.json` (quickFix.removeUserSpns sub-namespace in 5 languages with i18next plural variants)

**Created:**
- `src-tauri/src/services/spn.rs` (system-SPN guard module with 19 unit tests)
- `src/utils/spn.ts` (TS mirror of the Rust helper)
- `src/utils/spn.test.ts` (21 verbatim-port tests)
- `src/components/dialogs/ManageSpnsDialog.tsx` (custom dialog with removable/system grouping + useMfaGate wrapping + RemoveSpnsResult interface export)
- `src/components/dialogs/ManageSpnsDialog.test.tsx` (12 component tests)

## QA Results

(QA agent fills this section after the story is implemented.)
