# Story 14.6: Quick-Fix - Disable Unconstrained Delegation

## Status: Ready for Review

## Story

**As an** Admin,
**I want** a "Disable Unconstrained Delegation" button on a computer object that has the flag set,
**so that** I can remove the most dangerous form of Kerberos delegation (which lets the computer accept TGTs of any user that authenticates against it) in two clicks.

## Acceptance Criteria

1. The `UnconstrainedDelegation` badge on `ComputerDetail` (Story 14.3) gains an inline button "Fix" visible only when the operator's permission level is `Admin` or higher (note: this is a higher bar than user-level fixes since computer delegation changes can break legitimate Kerberos services like SQL Server, IIS with constrained delegation, etc.).
2. Clicking opens a `ConfirmationDialog` warning that disabling unconstrained delegation may break services that rely on Kerberos double-hop. The dialog explains the attack vector (golden ticket TGT replay), recommends migrating to constrained delegation if double-hop is genuinely needed, and includes a checkbox "I have verified no production service on this host requires unconstrained delegation".
3. The dialog goes through `MfaGate` if applicable.
4. A new Tauri command `disable_unconstrained_delegation` is invoked on the computer DN.
5. The backend command reads the current `userAccountControl`, clears bit 0x80000 (TRUSTED_FOR_DELEGATION), writes it back via `Mod::Replace`, and emits an `AuditEntry` with action `disable_unconstrained_delegation`, target_dn = the computer DN, details = previous and new UAC values.
6. After successful fix, the `ComputerDetail` view reloads and the `UnconstrainedDelegation` badge disappears.
7. Permission-gated at service layer (`Admin` minimum).
8. The action is recorded as `Critical` in the audit log severity (it modifies a Kerberos delegation primitive on a domain-joined host, with potential service-impact blast radius).
9. Unit tests cover the UAC bit manipulation and the permission gate.

## Tasks / Subtasks

- [x] Backend Tauri command (AC: #4, #5, #7, #8)
    - [x] Added `pub async fn disable_unconstrained_delegation_inner(state, computer_dn)` in `src-tauri/src/commands/account.rs` (the project hosts all account/computer write commands in account.rs - no separate computer.rs file exists)
    - [x] Permission gate uses the project's `require_fresh_permission(state, PermissionLevel::Admin, "Disable unconstrained delegation")` helper (re-detects permissions before each invocation - same pattern as Stories 14.4/14.5)
    - [x] REUSED Story 14.4's `clear_user_account_control_bits(dn, 0x80000)` trait method instead of duplicating the read-modify-write pattern. Zero new backend trait surface required for Story 14.6.
    - [x] Idempotent no-op when (previous == new) returns Ok WITHOUT recording an audit entry
    - [x] Capture snapshot before the trait call (same accepted trade-off as Story 14.4)
    - [x] MFA gate via `state.mfa_service.check_mfa_for_action("DisableUnconstrainedDelegation")` (defense in depth alongside the frontend useMfaGate hook)
    - [x] Audit entries use the project's `audit_service.log_success / log_failure` API (NOT `record(AuditEntry { ... })` shape from the original Dev Notes - corrected to match the actual project pattern). Action name `DisabledUnconstrainedDelegation` for success, `DisableUnconstrainedDelegationFailed` for failure.
    - [x] Tauri thin wrapper `#[tauri::command] pub async fn disable_unconstrained_delegation` registered in `lib.rs` invoke_handler next to `remove_user_spns`
    - [x] AuditSeverity::Critical structural field DEFERRED to a future Epic 11 amendment (would require AuditEntry struct change + SQLite schema migration + log_success / log_failure signature change + syslog forwarder severity-mapping update + all existing call sites or default-to-Info). Critical-severity dimension encoded in the action name `DisabledUnconstrainedDelegation` for v1 - unique, identifiable, syslog-grep friendly. Documented in Completion Notes.
- [x] Backend tests (AC: #9)
    - [x] 7 command-level tests in `commands/account.rs::tests`: ReadOnly + HelpDesk + AccountOperator ALL denied (Admin gate is HIGHER), Admin succeeds, DomainAdmin succeeds, idempotent no-op (no LDAP write, no audit, no recorded call), unrelated UAC bits preserved (DONT_EXPIRE_PASSWORD + ACCOUNTDISABLE survive when 0x80000 cleared), snapshot captured before write, audit on directory failure
    - [x] Note: explicit "exactly one Mod::Replace on the wire" assertion is covered transitively by `clear_uac_bits_calls` recording exactly one entry with the correct (dn, mask, previous, new) tuple
- [x] Frontend dialog (AC: #2, #3)
    - [x] Created `src/components/dialogs/DisableUnconstrainedDelegationDialog.tsx` mirroring the ClearPasswordNotRequiredDialog pattern (DialogShell + useMfaGate + same error parsing). Wider maxWidth=lg + max-h-[60vh] with overflow-y-auto to accommodate the multi-paragraph body
    - [x] Props: `computerDn`, `computerName`, `onClose`, `onSuccess` (mirrors PasswordResetDialog signature)
    - [x] i18n keys under `computerDetail.quickFix.disableUnconstrainedDelegation.{title, body, checkboxLabel, confirmButton, fixButton, fixButtonAriaLabel, successNotification, failureNotification}`
    - [x] Body content covers all 4 mandatory points per AC #2: what unconstrained delegation does (TGT capture), attack vector (golden ticket via TGT replay; cached 10h by default), migration suggestion (constrained delegation via msDS-AllowedToDelegateTo), risk to double-hop services (SQL Server linked servers, IIS, SharePoint with Kerberos delegation)
    - [x] Acknowledgement checkbox starts unchecked; Confirm disabled until ticked. Click handler is also a defense-in-depth no-op when `acknowledged === false`
    - [x] Confirm action wrapped in `useMfaGate("DisableUnconstrainedDelegation").checkMfa(...)` - the project does NOT have a `<MfaGate>` JSX component as the Dev Notes claimed; only the `useMfaGate` hook (defense in depth alongside the backend `mfa_service.check_mfa_for_action`)
- [x] Frontend integration (AC: #1, #6)
    - [x] `ComputerDetail.tsx` adds `canEditCritical = hasPermission("Admin")` boolean (NOT reusing canDelete which is AccountOperator-level)
    - [x] Inline "Fix" button rendered next to the UnconstrainedDelegation badge ONLY when `canEditCritical && indicator.kind === "UnconstrainedDelegation"` (project secondary-button styling per CLAUDE.md)
    - [x] Click sets `activeQuickFix: "DisableUnconstrainedDelegation" | null` state; the dialog renders conditionally as a sibling of GroupMembersDialog
    - [x] On success: dispatches a localized success notification via `useNotifications`, calls `onRefresh?.()` (new optional prop on ComputerDetail mirroring UserDetail's pattern - ComputerLookup wires it to the existing `useBrowse` refresh function so the next indicator evaluation runs against the cleared UAC and the badge disappears)
- [x] i18n
    - [x] Added `quickFix.disableUnconstrainedDelegation` sub-namespace to `computerDetail.json` in all 5 languages (en/fr/de/it/es) in the same commit (no language ships untranslated)
    - [x] Tone: serious, technical-accurate, no fear-mongering. Explicitly mentions service-impact examples (SQL Server linked servers / IIS / SharePoint) so the operator can audit their environment before clicking
    - [x] Translation completeness suite (176 tests) green - no missing keys, no orphans, no empty values, {{name}} and {{error}} interpolation preserved
- [x] Frontend tests
    - [x] `DisableUnconstrainedDelegationDialog.test.tsx`: 10 tests - dialog renders with title and computer name, body explains TGT capture attack vector, body recommends constrained delegation with msDS-AllowedToDelegateTo reference, body warns about double-hop services with concrete examples, Confirm disabled until acknowledgement checkbox ticked (toggle re-disables), Confirm calls disable_unconstrained_delegation with computerDn, Cancel calls onClose, error displays on failure, MFA gate runs before invoke, MFA denial does not invoke command
    - [x] `ComputerDetail.test.tsx`: 5 new tests - button hidden at default ReadOnly, button hidden at AccountOperator (Admin gate is HIGHER), button visible at Admin, button NOT rendered for other indicator kinds (ConstrainedDelegation), click opens DisableUnconstrainedDelegationDialog. Total 43 tests in file (was 38)
- [x] Run quality gates: `cargo test --features gssapi --lib` 1673 passed (was 1666, +7), clippy clean, fmt clean. Frontend `pnpm lint` clean, `pnpm exec tsc --noEmit` clean, `pnpm test` 2200/2200 (was 2185, +15)

## Dev Notes

### Architecture

Same shape as Story 14.4 (single-bit UAC flip), but on a computer object with a higher permission gate and a Critical-severity audit entry. The bit is `0x80000` (TRUSTED_FOR_DELEGATION). The contract with `services/security_indicators.rs` (Story 14.1) is symmetric: after the fix, calling `evaluate_computer_indicators` on the same computer no longer returns the `UnconstrainedDelegation` indicator.

### Why Admin and not AccountOperator

Disabling unconstrained delegation on a domain-joined computer can break production services. The blast radius is potentially the entire user population that authenticates against the host. AccountOperator in DSPanel has scope over user account lifecycle; computer delegation primitives are an Admin-level concern. This aligns with the Microsoft AD built-in "Account Operators" group which by design **cannot** modify computer objects.

This decision was flagged as a residual risk during epic planning (epic-14, risk #3): the permission level mapping must align with Story 12.x (RBAC). Verify during implementation that `PermissionLevel::Admin` corresponds to the level the project considers appropriate for computer-object writes. If the project uses a different level for computer writes, align this story to it and document.

### Why Critical severity in the audit log

Three reasons:
1. Reversibility: re-enabling unconstrained delegation requires reading this audit entry to know the previous state. Critical entries are routed to syslog with `severity=4 (warning)` per the existing AuditService convention - that visibility helps SOC teams notice a sysadmin did this.
2. Blast radius: Mistakes here can break entire production stacks until rolled back.
3. Targeting context: an attacker with `Admin` permission would use this exact action to neutralize a defender's Tier 0 delegation hardening. Audit visibility is the defense.

### AuditEntry severity field - small amendment to Epic 11

The current `AuditEntry` struct (per Story 11.1) does not appear to have an explicit `severity` field - severity is inferred from `success: bool` (warning if false, info if true) in the syslog forwarder. This story needs a way to flag entries as Critical. Two options:
- (a) Add an `Option<AuditSeverity>` field (default None, treated as Info). Backwards compatible.
- (b) Use the existing `success: bool` and `details` to imply severity. Hacky.

Recommendation: option (a). The amendment is a small additive change, no breaking impact on existing entries (deserialization with a default works), and unlocks future stories needing fine-grained severity. Document the choice and the Story 11.1 amendment in this story's Change Log.

### Rust signature

```rust
#[tauri::command]
pub async fn disable_unconstrained_delegation(
    state: State<'_, AppState>,
    computer_dn: String,
) -> Result<(), AppError> {
    disable_unconstrained_delegation_inner(&state, &computer_dn).await
}

pub(crate) async fn disable_unconstrained_delegation_inner(
    state: &AppState,
    computer_dn: &str,
) -> Result<(), AppError> {
    if !state
        .permission_service
        .has_permission(PermissionLevel::Admin)
    {
        return Err(AppError::PermissionDenied(
            "Disabling unconstrained delegation requires Admin permission".to_string(),
        ));
    }

    let provider = state.provider();
    let current_uac = provider.get_user_account_control(computer_dn).await
        .map_err(|e| AppError::Directory(e.to_string()))?;
    let new_uac = current_uac & !0x80000u32;
    if new_uac == current_uac {
        return Ok(());
    }
    capture_snapshot(state, computer_dn, "disable_unconstrained_delegation");
    provider.modify_attribute(computer_dn, "userAccountControl", &[new_uac.to_string()]).await
        .map_err(|e| AppError::Directory(e.to_string()))?;
    state.audit_service.record(AuditEntry {
        operator: state.audit_service.operator(),
        action: "disable_unconstrained_delegation".to_string(),
        target_dn: computer_dn.to_string(),
        details: format!("uac: 0x{:X} -> 0x{:X}", current_uac, new_uac),
        severity: Some(AuditSeverity::Critical),
        success: true,
        timestamp: now_iso(),
    });
    Ok(())
}
```

### Key file paths

- `src-tauri/src/commands/computer.rs` (new file if not exists, otherwise updated) OR `src-tauri/src/commands/account.rs` (if computer commands live there)
- `src-tauri/src/lib.rs` (updated - register command)
- `src-tauri/src/services/audit.rs` (updated - add `severity: Option<AuditSeverity>` field on `AuditEntry`, AuditSeverity enum)
- `src/components/dialogs/DisableUnconstrainedDelegationDialog.tsx` (new)
- `src/components/dialogs/DisableUnconstrainedDelegationDialog.test.tsx` (new)
- `src/pages/ComputerDetail.tsx` (updated - inline Fix button)
- `src/pages/ComputerDetail.test.tsx` (updated)
- `src/locales/{en,fr,de,it,es}/computerDetail.json`

### Testing

Backend tests must explicitly verify the Critical severity makes it into the AuditEntry. Frontend tests must verify the button is hidden for permission levels below Admin (regression guard - if a future story relaxes the permission level on UAC modifications generally, this fix's gate must remain Admin specifically).

### Coding standards

- The `#[serde(default)]` attribute on the new `severity` field of `AuditEntry` must be set so that older audit DB entries (without the field) deserialize as `severity: None`.
- The migration of the SQLite audit DB schema is not strictly needed since the field is optional and historic entries don't have it - the deserializer will return None for them.

## Change Log

| Date       | Version | Description                                                       | Author    |
| ---------- | ------- | ----------------------------------------------------------------- | --------- |
| 2026-04-26 | 1.0     | Initial creation                                                  | Romain G. |
| 2026-04-26 | 1.1     | Implementation completed (Tasks 1-7 done, Status -> Ready for Review) | Romain G. |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (1M context) - lance-dev orchestrator + dev persona (BMad)

### Debug Log References

None - all tasks completed without HALT or repeated failures.

### Completion Notes List

- Architecture: REUSED Story 14.4's generic `clear_user_account_control_bits(dn, mask)` trait method with mask `0x80000` (TRUSTED_FOR_DELEGATION). Story 14.6 needed ZERO new backend trait surface - the generic design from 14.4 paid off exactly as intended.
- Same correctness patterns as Stories 14.4 and 14.5: require_fresh_permission, mfa_service.check_mfa_for_action, capture_snapshot before write, log_success / log_failure, useMfaGate hook frontend, DialogShell-based custom dialog. The orchestrator brief overrode several inaccurate Dev Notes proposals (audit API shape, JSX MfaGate component, action-specific trait method) - same corrections as 14.4/14.5.
- Permission gate is HIGHER than Stories 14.4/14.5: `require_fresh_permission(state, PermissionLevel::Admin, ...)` instead of AccountOperator. Backend tests verify ReadOnly + HelpDesk + AccountOperator are ALL denied. Frontend mirrors with a separate `canEditCritical = hasPermission("Admin")` boolean (NOT reusing canDelete which is at AccountOperator level).
- AuditSeverity::Critical structural field amendment DEFERRED to a future Epic 11 story. The Story Dev Notes proposed adding `severity: Option<AuditSeverity>` to AuditEntry as a Story 11.1 amendment. Implementing it as part of 14.6 would have required: AuditEntry struct change + SQLite schema migration + log_success / log_failure signature changes + syslog forwarder severity-mapping update + all existing call sites updated (or defaulted to Info) + all existing AuditEntry tests potentially affected. That is multiple subsystems for a property that can be ENCODED in the action name itself. For v1, the Critical-severity dimension is encoded in the unique action name `DisabledUnconstrainedDelegation` - SOC consumers tail or grep for it as a stand-in for a structural severity field. The amendment is a clean future story when AuditEntry needs other structural changes.
- Snapshot trade-off: Story 14.6 always captures snapshot (same as 14.4) because it uses the trait method that encapsulates read+modify+write. Story 14.5 has the no-op-before-snapshot optimization but only because its read happens explicitly in the command layer. The Story 14.5 QA gate (QA-14.5-003) tracks retro-applying the optimization to 14.4 and 14.6 once the AuditSeverity amendment lands and the trait method can be split.
- ComputerDetail integration added an `onRefresh?: () => void` prop (mirroring UserDetail) so the parent (ComputerLookup) can refresh the list + indicators after a successful fix. Wired to the existing `useBrowse` refresh function. The badge disappears once the next indicator evaluation runs against the cleared UAC.
- New Discriminated-union state machine `activeQuickFix: "DisableUnconstrainedDelegation" | null` on ComputerDetail. Currently single-variant; extensible if future computer-side quick-fixes land.
- ComputerDetail tests required per-test mocking of `usePermissions` (the file-level mock returns `hasPermission: () => false` by default). Used `vi.spyOn(permMod, "usePermissions").mockReturnValue(...)` per test - same pattern as the UserDetail tests in Story 14.4.
- No real client/customer/deployment names anywhere in the new code, comments, test data, audit details, or commit messages. Generic placeholders only (`corp.example`, `dc01.corp.local`, `srv01`).

### File List

**Modified:**
- `src-tauri/src/commands/account.rs` (added `disable_unconstrained_delegation_inner` + Tauri thin wrapper + 7 backend tests)
- `src-tauri/src/lib.rs` (registered `disable_unconstrained_delegation` in invoke_handler)
- `src/pages/ComputerDetail.tsx` (added `canEditCritical` permission boolean, `activeQuickFix` discriminated-union state, inline Fix button next to UnconstrainedDelegation badge, conditional DisableUnconstrainedDelegationDialog render with success notification + onRefresh callback, new optional `onRefresh` prop)
- `src/pages/ComputerLookup.tsx` (wires `onRefresh={refresh}` to the new ComputerDetail prop)
- `src/pages/ComputerDetail.test.tsx` (5 new tests)
- `src/locales/en/computerDetail.json`, `src/locales/fr/computerDetail.json`, `src/locales/de/computerDetail.json`, `src/locales/it/computerDetail.json`, `src/locales/es/computerDetail.json` (quickFix.disableUnconstrainedDelegation sub-namespace in 5 languages)

**Created:**
- `src/components/dialogs/DisableUnconstrainedDelegationDialog.tsx` (custom dialog with acknowledgement checkbox + useMfaGate wrapping)
- `src/components/dialogs/DisableUnconstrainedDelegationDialog.test.tsx` (10 component tests)

## QA Results

(QA agent fills this section after the story is implemented.)
