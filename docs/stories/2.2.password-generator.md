# Story 2.2: Secure Password Generator

## Status: Draft

## Story

**As a** HelpDesk operator,
**I want** a password generator with configurable criteria and breach detection,
**so that** I can create strong, uncompromised passwords for users.

## Acceptance Criteria

1. `PasswordGenerator` produces passwords matching configurable criteria: length, uppercase, lowercase, digits, special characters, and ambiguous character exclusion.
2. `HibpClient` checks generated passwords against the HIBP Pwned Passwords API using k-anonymity (only the first 5 characters of the SHA1 hash are sent).
3. If a password is found in HIBP, it is automatically regenerated (up to a configurable retry limit).
4. A standalone password generator tool is available as a page/tab in DSPanel, usable by all permission levels.
5. The generator is also integrated into the `PasswordResetDialog` from Story 2.1.
6. When the HIBP API is unreachable, the generator falls back to offline mode and displays a warning that breach checking is unavailable.
7. Unit tests cover generation logic, HIBP integration, offline fallback, and retry behavior.

## Tasks / Subtasks

- [ ] Task 1: Implement `PasswordGenerator` (AC: #1)
  - [ ] Create `PasswordService` trait interface with `GenerateAsync(PasswordOptions)` method
  - [ ] Create `PasswordGenerator` implementing `PasswordService` trait
  - [ ] Support configurable options: `Length`, `IncludeUppercase`, `IncludeLowercase`, `IncludeDigits`, `IncludeSpecial`, `ExcludeAmbiguous`
  - [ ] Use `rand` crate for cryptographic randomness
  - [ ] Ensure at least one character from each selected category is included
- [ ] Task 2: Implement `HibpClient` with k-anonymity (AC: #2, #3)
  - [ ] Create `HibpClient` trait interface with `IsPasswordPwnedAsync(string password)` method
  - [ ] Implement `HibpClient` using `reqwest` client via Tauri state management
  - [ ] Compute SHA1 hash of the password
  - [ ] Send first 5 characters to `https://api.pwnedpasswords.com/range/{first5SHA1}`
  - [ ] Parse response and compare suffix against remaining hash characters
  - [ ] Return breach count if found, zero if clean
- [ ] Task 3: Implement auto-retry on breach detection (AC: #3)
  - [ ] After generation, check against HIBP
  - [ ] If breached, regenerate up to `MaxRetries` (default 5)
  - [ ] If all retries fail, return last password with a breach warning
- [ ] Task 4: Create standalone password tool view (AC: #4)
  - [ ] Create `PasswordGeneratorComponent` with options bindings and generate command
  - [ ] Create `PasswordGeneratorView.tsx` with criteria controls, generate button, result display
  - [ ] Add copy-to-clipboard button for generated password
  - [ ] Show HIBP status indicator (clean/breached/unchecked)
- [ ] Task 5: Integrate in `PasswordResetDialog` (AC: #5)
  - [ ] Wire auto-generate button to `PasswordService trait.GenerateAsync()`
  - [ ] Display HIBP check result alongside generated password
- [ ] Task 6: Implement offline fallback (AC: #6)
  - [ ] Catch `HttpRequestException` and `TaskCanceledException` from HIBP calls
  - [ ] Set HIBP status to "Unavailable" and display warning banner
  - [ ] Allow password generation to proceed without breach check
- [ ] Task 7: Write tests (AC: #7)
  - [ ] Test password generation meets all configured criteria
  - [ ] Test ambiguous character exclusion (`0OlI1` etc.)
  - [ ] Test HIBP k-anonymity request format (only 5 chars sent)
  - [ ] Test suffix matching logic with mock HIBP response
  - [ ] Test retry on breached password
  - [ ] Test offline fallback when API unreachable
  - [ ] Test standalone React component commands and bindings

## Dev Notes

### HIBP k-Anonymity Protocol

The Have I Been Pwned API uses k-anonymity so the full password hash is never transmitted:

1. Compute `SHA1(password)` - e.g., `5BAA61E4C9B93F3F0682250B6CF8331B7EE68FD8`
2. Send GET request to `https://api.pwnedpasswords.com/range/5BAA6`
3. Response contains suffixes like `1E4C9B93F3F0682250B6CF8331B7EE68FD8:3861493`
4. Match the suffix portion of the local hash against returned suffixes
5. If matched, the count indicates how many breaches contain this password

### PasswordGenerator Options

```typescript
pub struct PasswordOptions(
    int Length = 16,
    bool IncludeUppercase = true,
    bool IncludeLowercase = true,
    bool IncludeDigits = true,
    bool IncludeSpecial = true,
    bool ExcludeAmbiguous = false);
```

Ambiguous characters: `0 O o l I 1 | ` (backtick). When `ExcludeAmbiguous` is true, these are removed from the character pools.

### reqwest client Registration

`HibpClient` receives `reqwest` client via constructor injection. Register in Tauri state using `services.Addreqwest client<HibpClient trait, HibpClient>()` with a `User-Agent` header set to `DSPanel/{version}` and a 5-second timeout.

### Testing

- Mock `HibpClient` trait to control breach responses in `PasswordGenerator` tests.
- Mock `reqwest mock handler` to test `HibpClient` request format and response parsing.
- Test file: `src-tauri/tests/Services/Password/PasswordGeneratorTests.rs`
- Test file: `src-tauri/tests/Services/Password/HibpClientTests.rs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
| 2026-03-13 | 1.1 | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
