# Story 2.3: Account Unlock and Enable/Disable

## Status: Draft

## Story

**As a** HelpDesk operator,
**I want** to unlock, enable, or disable a user account directly from the lookup view,
**so that** I can resolve access issues quickly without switching to external AD tools.

## Acceptance Criteria

1. The user detail view shows "Unlock", "Enable", and "Disable" action buttons, each gated to HelpDesk+ permission level.
2. "Unlock" is only visible/enabled when the account is currently locked out.
3. "Enable" is only visible/enabled when the account is currently disabled.
4. "Disable" is only visible/enabled when the account is currently enabled.
5. Each action triggers a confirmation dialog via `DialogService` trait before execution.
6. On success, the user detail view refreshes immediately to reflect the new account state.
7. On failure, a clear error message is displayed to the operator.
8. Every action - success or failure - is logged via `AuditService` trait.
9. Unit tests cover React component logic, button visibility states, confirmation flow, and audit logging.

## Tasks / Subtasks

- [ ] Task 1: Add operations to `DirectoryProvider` trait (AC: #2, #3, #4)
  - [ ] Add `UnlockAccountAsync(string userDn)` method
  - [ ] Add `EnableAccountAsync(string userDn)` method
  - [ ] Add `DisableAccountAsync(string userDn)` method
  - [ ] Implement using `ldap3 crate` modify operations on `userAccountControl` and `lockoutTime`
- [ ] Task 2: Create action buttons in `UserLookupView` (AC: #1, #2, #3, #4)
  - [ ] Add "Unlock" button bound to `UnlockCommand`
  - [ ] Add "Enable" button bound to `EnableCommand`
  - [ ] Add "Disable" button bound to `DisableCommand`
  - [ ] Bind button visibility to account state properties (`IsLockedOut`, `IsDisabled`)
- [ ] Task 3: Permission gating (AC: #1)
  - [ ] Wrap action buttons with `PermissionGate` requiring `HelpDesk` level
  - [ ] Hide all three buttons for `ReadOnly` users
- [ ] Task 4: Confirmation dialogs (AC: #5)
  - [ ] Show confirmation via `DialogService trait.ConfirmAsync()` before each action
  - [ ] Include target user display name and action description in dialog
  - [ ] Cancel aborts the operation without any side effects
- [ ] Task 5: UI refresh on success (AC: #6)
  - [ ] After successful operation, re-query user attributes from AD
  - [ ] Update all bound properties to reflect new state
  - [ ] Update button visibility based on new state
- [ ] Task 6: Audit logging (AC: #8)
  - [ ] Log `AccountUnlocked`, `AccountEnabled`, or `AccountDisabled` on success
  - [ ] Log failure events with error details
- [ ] Task 7: Error handling (AC: #7)
  - [ ] Catch LDAP exceptions and display user-friendly error messages
  - [ ] Handle network timeouts and permission denied scenarios
- [ ] Task 8: Write tests (AC: #9)
  - [ ] Test button visibility for locked/unlocked account states
  - [ ] Test button visibility for enabled/disabled account states
  - [ ] Test confirmation dialog cancellation aborts operation
  - [ ] Test successful unlock calls `DirectoryProvider trait.UnlockAccountAsync`
  - [ ] Test successful enable/disable calls correct provider method
  - [ ] Test UI refresh after successful operation
  - [ ] Test audit logging on success and failure
  - [ ] Test permission gating hides buttons for ReadOnly

## Dev Notes

### UserAccountControl Flags

Account state is managed via the `userAccountControl` LDAP attribute using bitwise flags:
- `ACCOUNTDISABLE = 0x0002` - set to disable, clear to enable
- Lockout is managed via the `lockoutTime` attribute - set to `0` to unlock

### DirectoryProvider trait Methods

```typescript
Task UnlockAccountAsync(string userDn);    // Sets lockoutTime = 0
Task EnableAccountAsync(string userDn);     // Clears ACCOUNTDISABLE flag
Task DisableAccountAsync(string userDn);    // Sets ACCOUNTDISABLE flag
```

### Button State Logic

The React component exposes computed properties that drive button visibility:
- `CanUnlock` - true when `IsLockedOut == true`
- `CanEnable` - true when `IsDisabled == true`
- `CanDisable` - true when `IsDisabled == false`

These properties update after each refresh via React state updates.

### Confirmation Dialog

`DialogService trait.ConfirmAsync(title, message)` returns `bool`. The message includes the target user's display name and a clear description of the action, e.g., "Are you sure you want to disable the account for John Smith (jsmith)?".

### Testing

- Mock `DirectoryProvider` trait, `DialogService` trait, `AuditService` trait, and `PermissionService` trait.
- Test file: `src-tauri/tests/components/UserLookupComponentTests.tsx` (or a dedicated test class for account actions).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
| 2026-03-13 | 1.1 | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
