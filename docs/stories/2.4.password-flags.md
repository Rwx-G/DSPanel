# Story 2.4: Password Flag Management

## Status: Draft

## Story

**As an** Account Operator,
**I want** to toggle password-related flags on user accounts with a dry-run preview,
**so that** I can manage password policies per user while understanding the impact before applying changes.

## Acceptance Criteria

1. The user detail view shows checkboxes for "Password Never Expires" and "User Cannot Change Password", gated to AccountOperator+ permission level.
2. Checkboxes reflect the current state of the user's `userAccountControl` flags.
3. Toggling a checkbox does not immediately apply the change - a "Save" button becomes enabled.
4. Clicking "Save" opens a dry-run preview dialog showing the before/after flag state.
5. The operator must confirm the dry-run preview to apply the change.
6. On success, the UI refreshes to reflect the new flag state.
7. Every flag change - success or failure - is logged via `IAuditService` with before/after values.
8. Unit tests cover ViewModel logic, dry-run preview, permission gating, and audit logging.

## Tasks / Subtasks

- [ ] Task 1: Add `SetPasswordFlagsAsync` to `IDirectoryProvider` (AC: #2, #6)
  - [ ] Add `SetPasswordFlagsAsync(string userDn, PasswordFlags flags)` method
  - [ ] Implement bitwise manipulation of `userAccountControl` attribute
  - [ ] Support `DONT_EXPIRE_PASSWD = 0x10000` and `PASSWD_CANT_CHANGE` (via security descriptor)
- [ ] Task 2: Create flag checkboxes in user detail view (AC: #1, #2, #3)
  - [ ] Add "Password Never Expires" checkbox bound to ViewModel property
  - [ ] Add "User Cannot Change Password" checkbox bound to ViewModel property
  - [ ] Track dirty state - enable "Save" button only when flags differ from original
- [ ] Task 3: Permission gating (AC: #1)
  - [ ] Wrap flag checkboxes with `PermissionGate` requiring `AccountOperator` level
  - [ ] Show as read-only labels for HelpDesk and ReadOnly users
- [ ] Task 4: Dry-run preview dialog (AC: #4, #5)
  - [ ] Create `PasswordFlagsPreviewDialog` showing before/after state
  - [ ] Display flag name, current value, and proposed value for each changed flag
  - [ ] Include "Apply" and "Cancel" buttons
  - [ ] Invoke via `IDialogService`
- [ ] Task 5: Audit logging (AC: #7)
  - [ ] Log `PasswordFlagsChanged` with before/after values on success
  - [ ] Log `PasswordFlagsChangeFailed` with error details on failure
- [ ] Task 6: Write tests (AC: #8)
  - [ ] Test checkbox reflects current AD flag state
  - [ ] Test dirty tracking enables Save button only when changed
  - [ ] Test dry-run preview shows correct before/after
  - [ ] Test cancel on preview does not apply changes
  - [ ] Test successful apply calls `IDirectoryProvider.SetPasswordFlagsAsync`
  - [ ] Test permission gating for ReadOnly, HelpDesk, and AccountOperator
  - [ ] Test audit logging includes before/after values

## Dev Notes

### UserAccountControl Flags

The relevant flags in the `userAccountControl` attribute:
- `DONT_EXPIRE_PASSWD = 0x10000` (65536) - password never expires
- `PASSWD_CANT_CHANGE` - this flag is not directly in `userAccountControl`; it is enforced via ACL (deny Change Password to "Everyone" and "SELF"). The implementation must modify the security descriptor rather than the UAC attribute.

### PasswordFlags Model

```csharp
public record PasswordFlags(
    bool PasswordNeverExpires,
    bool UserCannotChangePassword);
```

### Dry-Run Preview

The dry-run dialog shows a simple table:

| Flag | Current | Proposed |
|------|---------|----------|
| Password Never Expires | No | Yes |

Only changed flags appear in the preview. If no flags have changed, the "Save" button remains disabled.

### PASSWD_CANT_CHANGE Complexity

Setting "User Cannot Change Password" requires modifying the object's DACL to add deny ACEs for the "Change Password" extended right (GUID `ab721a53-1e2f-11d0-9819-00aa0040529b`) for "Everyone" and "SELF". This is more complex than a simple attribute modify and should be implemented carefully with proper error handling.

### Testing

- Mock `IDirectoryProvider` and verify correct `PasswordFlags` argument.
- Mock `IDialogService` to simulate preview confirm/cancel.
- Test file: `src/DSPanel.Tests/ViewModels/PasswordFlagsViewModelTests.cs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
