# Story 3.1: Side-by-Side User Comparison

## Status: Draft

## Story

**As a** HelpDesk operator,
**I want** to compare two AD users side by side with a group membership delta,
**so that** I can quickly identify permission differences and troubleshoot access issues.

## Acceptance Criteria

1. A "Compare" tab/page allows selecting two users via search fields.
2. Both users' key attributes are displayed in parallel columns (display name, title, department, OU, last logon, account status).
3. Group memberships are compared with color-coded indicators: green for shared groups, red for groups only in User A, blue for groups only in User B.
4. A delta summary shows counts: total shared, only-A, only-B.
5. Group lists support filtering by name and sorting by name or membership type (shared/unique).
6. The comparison view opens as a new tab in the main navigation.
7. Unit tests cover the comparison logic, delta computation, and React component bindings.

## Tasks / Subtasks

- [ ] Task 1: Create `ComparisonComponent` (AC: #1, #2, #3, #4)
  - [ ] Add `UserA` and `UserB` React state properties (type `DirectoryUser`)
  - [ ] Add `SearchUserACommand` and `SearchUserBCommand` for user selection
  - [ ] Add `CompareCommand` that triggers delta computation
  - [ ] Compute `SharedGroups`, `OnlyAGroups`, `OnlyBGroups` collections
  - [ ] Compute `DeltaSummary` with counts
- [ ] Task 2: Create `ComparisonView.tsx` (AC: #1, #2, #6)
  - [ ] Dual-panel layout with user search fields at the top
  - [ ] Left panel: User A attributes and group list
  - [ ] Right panel: User B attributes and group list
  - [ ] Center/bottom panel: delta summary
- [ ] Task 3: Implement group diff algorithm (AC: #3)
  - [ ] Compare group DNs (case-insensitive) to produce three sets
  - [ ] Handle nested group memberships (use `tokenGroups` or recursive expansion)
  - [ ] Account for primary group membership
- [ ] Task 4: Color-coded group display (AC: #3)
  - [ ] Green background/icon for shared groups
  - [ ] Red background/icon for groups exclusive to User A
  - [ ] Blue background/icon for groups exclusive to User B
  - [ ] Use React component template with color binding based on membership category
- [ ] Task 5: Delta summary panel (AC: #4)
  - [ ] Display total group count per user
  - [ ] Display shared count, only-A count, only-B count
  - [ ] Visual bar or badges with matching colors
- [ ] Task 6: Filter and sort (AC: #5)
  - [ ] Add text filter for group name substring search
  - [ ] Add sort options: alphabetical, by membership type
  - [ ] Use React filter and sort hooks for filtered/sorted views
- [ ] Task 7: Tab integration (AC: #6)
  - [ ] Register `ComparisonView` as a navigable page in the main tab control
  - [ ] Add "Compare" option to the navigation menu
- [ ] Task 8: Write tests (AC: #7)
  - [ ] Test group diff with overlapping memberships
  - [ ] Test group diff with no overlap
  - [ ] Test group diff with identical memberships
  - [ ] Test delta summary counts
  - [ ] Test filter reduces displayed groups
  - [ ] Test sort ordering

## Dev Notes

### Group Diff Algorithm

The comparison uses set operations on group distinguished names (case-insensitive):
- `SharedGroups = groupsA.Intersect(groupsB, StringComparer.OrdinalIgnoreCase)`
- `OnlyAGroups = groupsA.Except(groupsB, StringComparer.OrdinalIgnoreCase)`
- `OnlyBGroups = groupsB.Except(groupsA, StringComparer.OrdinalIgnoreCase)`

### Group Resolution

Use `DirectoryProvider trait.GetUserGroupsAsync(userDn)` which should return all effective group memberships. This can leverage the `tokenGroups` constructed attribute for recursive resolution or perform manual recursive expansion of `memberOf`.

### View Layout

```
+-------------------+-------------------+
| [Search User A]   | [Search User B]   |
+-------------------+-------------------+
| Name: John Smith  | Name: Jane Doe    |
| Title: Engineer   | Title: Manager    |
| Dept: IT          | Dept: IT          |
| ...               | ...               |
+-------------------+-------------------+
| Groups (A)        | Groups (B)        |
| [green] Group1    | [green] Group1    |
| [red]   Group2    | [blue]  Group4    |
| [green] Group3    | [green] Group3    |
+-------------------+-------------------+
| Summary: 2 shared | 1 only-A | 1 only-B |
+---------------------------------------+
```

### Testing

- Create test `DirectoryUser` objects with known group lists.
- Test file: `src-tauri/tests/components/ComparisonComponentTests.tsx`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
| 2026-03-13 | 1.1 | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
