# Story 3.3: NTFS Permissions Analyzer

## Status: Draft

## Story

**As an** IT administrator,
**I want** a standalone NTFS permissions analyzer that shows inherited vs explicit ACEs with full group resolution,
**so that** I can audit folder permissions in depth without relying on external tools.

## Acceptance Criteria

1. A standalone "NTFS Analyzer" page is available in the navigation menu.
2. The operator enters a UNC path and the tool displays all ACEs with clear indication of inherited vs explicit.
3. For each ACE trustee that is a group, the full resolution chain is shown (group members, nested groups).
4. Recursive analysis allows scanning a folder and its subfolders to a configurable depth.
5. Deny rules are highlighted with a distinct visual style (e.g., red background, warning icon).
6. Conflicts between allow and deny at different levels are flagged.
7. Results can be exported to CSV and PDF.
8. Unit tests cover ACL resolution, group chain expansion, conflict detection, and export.

## Tasks / Subtasks

- [ ] Task 1: Create `NtfsAnalyzerComponent` (AC: #1, #2)
  - [ ] Add `UncPath` React state property
  - [ ] Add `AnalyzeCommand` bound to the analyze button
  - [ ] Add `AceEntries` React state array for results
  - [ ] Add `RecursiveDepth` property (default 0 - current folder only)
  - [ ] Add `IsAnalyzing` property for loading state
- [ ] Task 2: Create standalone `NtfsAnalyzerView.tsx` (AC: #1)
  - [ ] UNC path input with browse/analyze button
  - [ ] Depth selector for recursive analysis
  - [ ] React data table displaying ACE entries with grouping by path
  - [ ] Register as a navigable page in main navigation
- [ ] Task 3: Implement recursive ACL resolution (AC: #4)
  - [ ] Enumerate subdirectories to configurable depth
  - [ ] Read ACLs for each directory
  - [ ] Aggregate results with path context
  - [ ] Handle access denied on individual subdirectories gracefully
- [ ] Task 4: Group chain resolution (AC: #3)
  - [ ] For each ACE trustee that is a group, query members from AD
  - [ ] Recursively expand nested groups
  - [ ] Display as expandable tree: Group - Subgroup - User
  - [ ] Cache resolved groups to avoid redundant LDAP queries
- [ ] Task 5: Display inherited vs explicit (AC: #2)
  - [ ] Use distinct icons or labels for inherited and explicit ACEs
  - [ ] Show inheritance source path for inherited ACEs when available
  - [ ] Allow filtering to show only explicit ACEs
- [ ] Task 6: Highlight deny rules and conflicts (AC: #5, #6)
  - [ ] Apply red background or warning icon to deny ACEs
  - [ ] Detect conflicts: allow at parent level, deny at child level (or vice versa) for same trustee
  - [ ] Flag conflicts with a warning indicator and tooltip explanation
- [ ] Task 7: CSV and PDF export (AC: #7)
  - [ ] CSV export: Path, Trustee, Type, Permissions, Inherited, Source
  - [ ] PDF export: formatted table with color coding preserved
  - [ ] Use `DialogService` trait for file save dialog
- [ ] Task 8: Write tests (AC: #8)
  - [ ] Test ACL resolution returns correct ACEs
  - [ ] Test inherited vs explicit classification
  - [ ] Test group chain expansion with nested groups
  - [ ] Test recursive scan aggregates results by path
  - [ ] Test deny rule highlighting
  - [ ] Test conflict detection between allow and deny
  - [ ] Test CSV export format
  - [ ] Test graceful handling of access denied on subdirectories

## Dev Notes

### Recursive Scanning

Recursive analysis uses `Directory.EnumerateDirectories()` with a depth counter. Each directory is processed independently so that an access denied error on one subdirectory does not abort the entire scan. Failed directories are logged and included in results with an error indicator.

### Group Chain Resolution

When an ACE trustee is a group SID, resolve it via `DirectoryProvider trait.GetGroupMembersAsync(groupDn)`. For nested groups, recursively expand until only users remain. Display as a tree:

```
Trustee: IT-Department (group)
  - IT-Admins (group)
    - jsmith (user)
    - jdoe (user)
  - IT-Support (group)
    - bwilson (user)
```

Cache resolved groups in a `Dictionary<string, IList<DirectoryObject>>` to avoid duplicate LDAP queries during the same analysis session.

### Conflict Detection

A conflict exists when:
- The same trustee (or a group containing the same user) has both Allow and Deny ACEs for overlapping permissions
- A deny at a child folder overrides an allow inherited from a parent

Flag these with a warning icon and a tooltip explaining the conflict.

### PDF Export

Use a lightweight PDF library to generate formatted tables. Consider `printpdf crate` or `printpdf crate` for report generation. The PDF should include a header with the analysis date, scanned path, and operator name.

### Testing

- Mock `NtfsPermissionService` trait for ACL data.
- Mock `DirectoryProvider` trait for group resolution.
- Test files:
  - `src-tauri/tests/components/NtfsAnalyzerComponentTests.tsx`
  - `src-tauri/tests/Services/Ntfs/NtfsPermissionServiceTests.rs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
| 2026-03-13 | 1.1 | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |

## QA Results

### Review Date: 2026-03-14

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Clean implementation of the standalone NTFS analyzer with recursive scanning, conflict detection, and group-by-path display. The `ntfs_analyzer.rs` service has well-separated concerns: directory enumeration, ACL reading per path, and conflict detection. The `PathAclSection` component with collapsible paths and `table-fixed` layout is well-polished.

**Strengths:**
- Recursive scanning with configurable depth and per-directory error handling
- Conflict detection algorithm correctly identifies same-trustee allow/deny across paths
- Fixed column widths prevent layout shift across multiple path sections
- Enter key triggers analysis, consistent with UX patterns
- Right-click "View group members" on trustees

**Concerns:**
- AC #3 (group chain resolution with expandable tree) is not implemented - trustees show names but not their member hierarchy
- AC #7 mentions PDF export - only CSV export is implemented

### Refactoring Performed

None.

### Compliance Check

- Coding Standards: ✓
- Project Structure: ✓ ntfs_analyzer.rs service, NtfsAnalyzer page, ntfs-analyzer types
- Testing Strategy: ✓ 10 Rust tests for conflict detection/analysis, 8 component tests
- All ACs Met: Partial - AC #3 group chain not implemented, AC #7 PDF export missing

### Improvements Checklist

- [x] Standalone page in navigation (AC #1)
- [x] Inherited vs explicit indication (AC #2)
- [x] Recursive analysis with configurable depth (AC #4)
- [x] Deny rule highlighting (AC #5)
- [x] Conflict detection between allow/deny (AC #6)
- [x] CSV export (AC #7 partial)
- [x] Explicit-only filter toggle
- [x] Right-click "View group members" on trustees
- [ ] Group chain resolution with expandable tree (AC #3)
- [ ] PDF export (AC #7 partial)

### Security Review

No concerns. UNC path validation prevents path injection.

### Performance Considerations

Recursive scanning could be slow on deep directory trees. The configurable depth limit (max 5) mitigates this.

### Files Modified During Review

None.

### Gate Status

Gate: CONCERNS -> docs/qa/gates/3.3-ntfs-analyzer.yml

### Recommended Status

✗ Changes Required - AC #3 (group chain) and AC #7 (PDF export) partially missing
