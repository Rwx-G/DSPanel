# Story 4.1: Group Browser (Tree and Flat View)

## Status: Draft

## Story

**As an** IT administrator,
**I want** to browse AD groups in both a hierarchical OU tree and a flat searchable list,
**so that** I can quickly find and inspect any group in the domain.

## Acceptance Criteria

1. A "Group Management" page is available in the main navigation.
2. A tree view displays groups organized by Organizational Unit hierarchy, with lazy loading on OU expansion.
3. A flat view displays all groups in a searchable, sortable list.
4. A toggle allows switching between tree and flat views.
5. Selecting a group displays its details: name, distinguished name, description, scope (Global/DomainLocal/Universal), category (Security/Distribution), and member count.
6. The detail panel shows a member list with type icons (user, group, computer) for each member.
7. Search works across both views - filtering the tree or the flat list by group name.
8. Unit tests cover tree construction, flat list filtering, detail loading, and member display.

## Tasks / Subtasks

- [ ] Task 1: Create `GroupManagementViewModel` (AC: #1, #2, #3, #4, #5)
  - [ ] Add `ViewMode` property (Tree/Flat) with toggle command
  - [ ] Add `OuTree` observable collection for tree view (lazy-loaded OU nodes)
  - [ ] Add `FlatGroups` observable collection for flat view
  - [ ] Add `SelectedGroup` property with detail loading
  - [ ] Add `SearchText` property with filter logic
  - [ ] Add `GroupDetail` property exposing name, DN, description, scope, category, member count
- [ ] Task 2: Create `GroupManagementView.xaml` (AC: #1, #4, #6)
  - [ ] Left panel: tree/flat view toggle and group browser
  - [ ] Tree view with `TreeView` control and `HierarchicalDataTemplate`
  - [ ] Flat view with `ListView` or `DataGrid`
  - [ ] Right panel: group detail and member list
  - [ ] Register as a navigable page in main navigation
- [ ] Task 3: Implement OU tree with lazy loading (AC: #2)
  - [ ] Query root OUs from AD on page load
  - [ ] On OU node expansion, query child OUs and groups within that OU
  - [ ] Use dummy child node for expandable indicator, replace on expand
  - [ ] Show loading indicator during LDAP query
- [ ] Task 4: Implement flat list with filtering (AC: #3, #7)
  - [ ] Query all groups from AD (with paging for large domains)
  - [ ] Implement client-side filtering on `SearchText` change
  - [ ] Use `CollectionViewSource` for efficient filtering
  - [ ] Support sorting by name, scope, and category
- [ ] Task 5: Group detail panel (AC: #5)
  - [ ] Display group attributes: `cn`, `distinguishedName`, `description`, `groupType` (parsed to scope and category)
  - [ ] Compute and display member count
  - [ ] Parse `groupType` flags: scope (Global=2, DomainLocal=4, Universal=8) and category (Security=0x80000000)
- [ ] Task 6: Member list with type icons (AC: #6)
  - [ ] Query group members via `IDirectoryProvider.GetGroupMembersAsync(groupDn)`
  - [ ] Determine member type from `objectClass`: user, group, or computer
  - [ ] Display with appropriate icon per type
  - [ ] Support pagination for groups with many members
- [ ] Task 7: Search across views (AC: #7)
  - [ ] In tree mode: filter visible nodes, expanding OUs that contain matches
  - [ ] In flat mode: standard substring filter on group name
  - [ ] Debounce search input (300ms) to avoid excessive queries
- [ ] Task 8: Write tests (AC: #8)
  - [ ] Test tree construction from OU hierarchy
  - [ ] Test lazy loading populates child nodes on expand
  - [ ] Test flat list filtering by name
  - [ ] Test group detail shows correct scope and category
  - [ ] Test member list groups members by type
  - [ ] Test search debouncing
  - [ ] Test toggle between tree and flat view

## Dev Notes

### GroupType Attribute Parsing

The `groupType` attribute is a bitwise integer:
- Scope bits: `GLOBAL = 0x2`, `DOMAIN_LOCAL = 0x4`, `UNIVERSAL = 0x8`
- Category bit: `SECURITY = 0x80000000` (if set, Security group; if not, Distribution group)

Example: `groupType = -2147483646` (0x80000002) means Global Security group.

### Lazy Loading Pattern

```csharp
public class OuTreeNode : ObservableObject
{
    public string Dn { get; }
    public string Name { get; }
    public ObservableCollection<OuTreeNode> Children { get; }
    public bool IsExpanded { get; set; } // triggers lazy load
    private bool _isLoaded;

    // On first expand, replace dummy child with actual children
}
```

### LDAP Queries

- Root OUs: `(&(objectClass=organizationalUnit))` scoped to one level below domain root
- Child OUs: same filter, scoped to one level below selected OU
- Groups in OU: `(objectClass=group)` scoped to one level below OU
- All groups (flat): `(objectClass=group)` with paged results control

### Performance Considerations

Large domains can have thousands of groups. The flat view should use server-side paging (LDAP paged results control) and load incrementally. The tree view naturally limits queries via lazy loading.

### Testing

- Mock `IDirectoryProvider` to return controlled OU and group hierarchies.
- Test file: `src/DSPanel.Tests/ViewModels/GroupManagementViewModelTests.cs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
