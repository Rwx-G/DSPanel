# Story 4.2: Group Member Management

## Status: Draft

## Story

**As an** Account Operator,
**I want** to add and remove group members using drag-drop and multi-select with a dry-run preview,
**so that** I can manage group memberships efficiently while reviewing changes before they take effect.

## Acceptance Criteria

1. Drag-and-drop from user search results or other group member lists adds members to the selected group.
2. Multi-select allows batch adding of multiple users/groups at once.
3. Members can be removed via delete key or right-click context menu.
4. All add/remove operations show a dry-run preview before execution.
5. Every membership change is logged via `IAuditService` with operator, group, and member details.
6. Operations are gated to AccountOperator+ permission level.
7. A confirmation summary shows total adds and removes before applying.
8. Unit tests cover drag-drop logic, batch operations, dry-run preview, permission gating, and audit logging.

## Tasks / Subtasks

- [ ] Task 1: Implement drag-drop in group view (AC: #1)
  - [ ] Enable drag source on user search results and member lists
  - [ ] Enable drop target on group member panel
  - [ ] Extract dragged object DNs on drop
  - [ ] Validate drop target (prevent adding a group to itself, check for circular nesting)
- [ ] Task 2: Multi-select support (AC: #2)
  - [ ] Enable multi-select in user search results
  - [ ] Add "Add Selected to Group" command
  - [ ] Collect all selected DNs for batch operation
- [ ] Task 3: Remove members (AC: #3)
  - [ ] Handle Delete key press on selected member(s)
  - [ ] Add "Remove from Group" context menu item
  - [ ] Support multi-select removal
- [ ] Task 4: Dry-run preview dialog (AC: #4, #7)
  - [ ] Create `MemberChangePreviewDialog` showing pending changes
  - [ ] List each add/remove with member name, DN, and operation type
  - [ ] Show summary: "X members to add, Y members to remove"
  - [ ] Include "Apply" and "Cancel" buttons
  - [ ] Invoke via `IDialogService`
- [ ] Task 5: Permission gating (AC: #6)
  - [ ] Wrap add/remove operations with `PermissionGate` requiring `AccountOperator` level
  - [ ] Disable drag-drop target and context menu for HelpDesk and ReadOnly users
  - [ ] Show member list as read-only for insufficient permissions
- [ ] Task 6: Implement add/remove via `IDirectoryProvider` (AC: #1, #3)
  - [ ] Add `AddGroupMemberAsync(string groupDn, string memberDn)` to `IDirectoryProvider`
  - [ ] Add `RemoveGroupMemberAsync(string groupDn, string memberDn)` to `IDirectoryProvider`
  - [ ] Implement using LDAP modify operations on the `member` attribute
- [ ] Task 7: Audit logging (AC: #5)
  - [ ] Log `GroupMemberAdded` with group DN, member DN, and operator
  - [ ] Log `GroupMemberRemoved` with group DN, member DN, and operator
  - [ ] Log batch operations as individual entries per member
- [ ] Task 8: Write tests (AC: #8)
  - [ ] Test drag-drop adds member to correct group
  - [ ] Test multi-select batch add
  - [ ] Test remove via delete key and context menu
  - [ ] Test dry-run preview shows correct pending changes
  - [ ] Test cancel on preview does not apply changes
  - [ ] Test permission gating disables operations for ReadOnly and HelpDesk
  - [ ] Test audit logging for add and remove operations
  - [ ] Test circular nesting prevention

## Dev Notes

### Drag-Drop Implementation

WPF drag-drop uses `DragDrop.DoDragDrop()` on the source and `Drop` event handler on the target. The data object should carry a `List<string>` of member DNs. Use `IDataObject` with a custom format like `DSPanel.MemberDnList`.

### Batch Operation Flow

1. Operator drags users or clicks "Add Selected" - pending changes collected
2. Operator optionally removes members - pending changes collected
3. Operator clicks "Apply" - dry-run preview dialog opens
4. Preview shows all pending adds/removes
5. Operator confirms - changes applied sequentially
6. Each change is individually audited
7. On partial failure, completed changes are kept and errors are reported

### LDAP Member Modification

Adding a member to a group modifies the `member` attribute of the group object:
- Add: `ModifyRequest` with `DirectoryAttributeModification` of type `Add`
- Remove: `ModifyRequest` with `DirectoryAttributeModification` of type `Delete`

### Circular Nesting Prevention

Before adding a group as a member of another group, check if the target group is already a member (direct or nested) of the source group. This prevents circular nesting like A contains B contains A.

### Testing

- Mock `IDirectoryProvider` for add/remove operations.
- Mock `IDialogService` to simulate preview confirm/cancel.
- Test files:
  - `src/DSPanel.Tests/ViewModels/GroupMemberManagementTests.cs`
  - `src/DSPanel.Tests/ViewModels/Dialogs/MemberChangePreviewTests.cs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
