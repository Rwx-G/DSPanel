# Story 7.3: Contact and Printer Management

## Status: Draft

## Story

**As an** administrator,
**I want** to view, create, edit, and delete AD contacts and printers from DSPanel,
**so that** I can manage all directory object types from a single tool.

## Acceptance Criteria

1. Contacts appear in search results alongside users, computers, and groups, distinguished by a contact icon.
2. A contact detail view shows all relevant attributes (display name, email, phone, company, address).
3. CRUD operations on contacts are available for AccountOperator+ users.
4. Printers appear in search results with a printer icon and show name, location, server, and share path.
5. Printer detail view displays all printer attributes in read-only mode for all users.
6. Printer management (create, edit, delete) is gated to DomainAdmin.
7. All create, update, and delete operations are logged via `IAuditService`.
8. Unit tests cover search integration, CRUD operations, permission gating, and audit logging.

## Tasks / Subtasks

- [ ] Task 1: Extend search to include contacts and printers (AC: #1, #4)
  - [ ] Update `IDirectoryProvider.SearchAsync()` filter to include `(objectClass=contact)` and `(objectClass=printQueue)`
  - [ ] Add `Contact` and `Printer` icons to search result type indicators
  - [ ] Map search results to `ContactInfo` and `PrinterInfo` models
- [ ] Task 2: Create contact detail view (AC: #2)
  - [ ] Display: display name, first/last name, email, phone, mobile, company, department, street address, city, state, zip, country
  - [ ] Read-only mode by default
  - [ ] Edit mode for AccountOperator+ users
- [ ] Task 3: Contact CRUD operations (AC: #3)
  - [ ] Create: dialog form for new contact, calls `IDirectoryProvider.CreateObjectAsync()` with objectClass=contact
  - [ ] Update: edit form saves modified attributes via `IDirectoryProvider.ModifyAttributesAsync()`
  - [ ] Delete: confirmation dialog, then `IDirectoryProvider.DeleteObjectAsync()`
  - [ ] Gate all write operations to AccountOperator+
- [ ] Task 4: Printer detail view (AC: #4, #5)
  - [ ] Display: printer name, location, server name, share name, driver name, port name, description
  - [ ] Read-only for all users
  - [ ] Parse `uNCName` for share path, `serverName` for print server
- [ ] Task 5: Printer management (AC: #6)
  - [ ] Create/edit/delete printer objects in AD
  - [ ] Gate to DomainAdmin
  - [ ] Confirmation dialog for delete
- [ ] Task 6: Audit logging (AC: #7)
  - [ ] Log all CRUD operations for contacts and printers
  - [ ] Include object type, DN, and operation details
- [ ] Task 7: Write tests (AC: #8)
  - [ ] Test search includes contacts and printers with correct icons
  - [ ] Test contact detail view population
  - [ ] Test contact create/update/delete calls correct provider methods
  - [ ] Test printer detail view population
  - [ ] Test printer management gated to DomainAdmin
  - [ ] Test contact management gated to AccountOperator
  - [ ] Test audit logging for all CRUD operations

## Dev Notes

### LDAP Object Classes

| Object | LDAP Filter | Key Attributes |
|--------|------------|----------------|
| Contact | `(objectClass=contact)` | `cn`, `displayName`, `givenName`, `sn`, `mail`, `telephoneNumber`, `mobile`, `company`, `department`, `streetAddress`, `l`, `st`, `postalCode`, `co` |
| Printer | `(objectClass=printQueue)` | `printerName`, `location`, `serverName`, `uNCName`, `driverName`, `portName`, `description` |

### Search Integration

The existing search query is extended with additional objectClass conditions. The search result model gains a `ResultType` enum value for `Contact` and `Printer`, which drives icon selection in the UI.

### File Layout

- `Models/ContactInfo.cs`
- `Models/PrinterInfo.cs`
- `ViewModels/ContactDetailViewModel.cs`
- `ViewModels/PrinterDetailViewModel.cs`
- `Views/Pages/ContactDetailView.xaml`
- `Views/Pages/PrinterDetailView.xaml`

### Testing

- Use `Moq` to mock `IDirectoryProvider`, `IDialogService`, `IAuditService`, `IPermissionService`.
- Use `FluentAssertions` for readable assertions.
- Test files:
  - `src/DSPanel.Tests/ViewModels/ContactDetailViewModelTests.cs`
  - `src/DSPanel.Tests/ViewModels/PrinterDetailViewModelTests.cs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
