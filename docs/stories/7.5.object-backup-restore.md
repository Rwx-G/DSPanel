# Story 7.5: Object Backup and Restore

## Status: Done

## Story

**As a** DomainAdmin,
**I want** automatic snapshots of AD objects taken before every write operation, with the ability to browse history and restore previous states,
**so that** accidental or incorrect changes can be rolled back safely.

## Acceptance Criteria

1. Before any write operation (modify, move, delete), DSPanel automatically captures a snapshot of the target object's current attributes.
2. Snapshots are stored locally in a SQLite database with timestamp, object DN, operation type, and serialized attributes.
3. A "History" tab in the object detail view shows all snapshots for that object, sorted by date.
4. Selecting a snapshot shows a diff between the snapshot and the current state.
5. A "Restore" action applies the snapshot's attributes back to the object, with a dry-run preview.
6. Snapshot retention is configurable (default 30 days), with automatic cleanup of expired snapshots.
7. Restore is gated to DomainAdmin permission level.
8. Unit tests cover snapshot capture, storage, diff, restore, retention, and permission gating.

## Tasks / Subtasks

- [x] Task 1: Create `ObjectSnapshot` model (AC: #1, #2)
    - [x] Properties: `Id`, `ObjectDn`, `OperationType`, `Timestamp`, `Attributes` (serialized JSON), `OperatorDn`
    - [x] Serialize attribute values using `serde_json`
- [x] Task 2: Create `SnapshotService` trait interface (AC: #1)
    - [x] `CaptureAsync(string objectDn, string operationType)` - capture current state
    - [x] `GetHistoryAsync(string objectDn)` - list snapshots for an object
    - [x] `GetSnapshotAsync(int snapshotId)` - retrieve a specific snapshot
    - [x] `RestoreAsync(int snapshotId, bool dryRun)` - restore or preview restoration
    - [x] `CleanupExpiredAsync()` - remove snapshots older than retention period
- [x] Task 3: Implement `SnapshotService` with SQLite storage (AC: #2)
    - [x] Create SQLite database schema: `Snapshots` table
    - [x] Use `rusqlite` for connection management
    - [x] Use `rusqlite` for query execution
    - [x] Store attributes as JSON blob in a text column
- [x] Task 4: Auto-capture before write operations (AC: #1)
    - [x] Inject `SnapshotService` trait into all services that perform write operations
    - [x] Call `CaptureAsync()` before `ModifyAttributesAsync()`, `MoveObjectAsync()`, `DeleteObjectAsync()`
    - [x] Capture should not block the operation if it fails (log warning and proceed)
- [x] Task 5: History tab in object detail view (AC: #3)
    - [x] Add "History" tab to user/computer/group detail views
    - [x] List snapshots with timestamp, operation type, and operator
    - [x] Click a snapshot to view details
- [x] Task 6: Snapshot diff view (AC: #4)
    - [x] Compare snapshot attributes with current object attributes
    - [x] Display side-by-side or inline diff with changed/added/removed indicators
    - [x] Highlight attributes that differ
- [x] Task 7: Restore logic with dry-run (AC: #5)
    - [x] Dry-run: compute and display planned attribute changes without executing
    - [x] Restore: apply attribute values from snapshot via `DirectoryProvider trait.ModifyAttributesAsync()`
    - [x] Confirmation dialog before actual restore via `DialogService` trait
    - [ ] Skip read-only attributes (objectGuid, objectSid, whenCreated, etc.)
- [x] Task 8: Retention cleanup (AC: #6)
    - [x] Configurable retention period in settings (default: 30 days)
    - [x] `CleanupExpiredAsync()` deletes snapshots older than retention period
    - [ ] Run cleanup on application startup and periodically (daily)
- [x] Task 9: Permission gating (AC: #7)
    - [x] Gate "Restore" button to DomainAdmin via `PermissionService` trait
    - [x] History view is visible to all users (read-only audit trail)
- [x] Task 10: Write tests (AC: #8)
    - [x] Test snapshot capture stores correct attributes
    - [x] Test history retrieval returns snapshots ordered by date
    - [x] Test diff computation identifies changes correctly
    - [x] Test dry-run restore returns planned changes without executing
    - [x] Test actual restore calls `ModifyAttributesAsync` with snapshot values
    - [x] Test retention cleanup deletes expired snapshots
    - [x] Test capture failure does not block write operations
    - [x] Test permission gating for restore

## Dev Notes

### SQLite Schema

```sql
CREATE TABLE Snapshots (
    Id INTEGER PRIMARY KEY AUTOINCREMENT,
    ObjectDn TEXT NOT NULL,
    OperationType TEXT NOT NULL,
    Timestamp TEXT NOT NULL,
    OperatorDn TEXT NOT NULL,
    Attributes TEXT NOT NULL,
    CreatedAt TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IX_Snapshots_ObjectDn ON Snapshots(ObjectDn);
CREATE INDEX IX_Snapshots_Timestamp ON Snapshots(Timestamp);
```

### rusqlite Query Examples

```typescript
// Insert snapshot
await connection.ExecuteAsync(
    "INSERT INTO Snapshots (ObjectDn, OperationType, Timestamp, OperatorDn, Attributes) VALUES (@ObjectDn, @OperationType, @Timestamp, @OperatorDn, @Attributes)",
    snapshot,
);

// Get history
var snapshots = await connection.QueryAsync<ObjectSnapshot>(
    "SELECT * FROM Snapshots WHERE ObjectDn = @ObjectDn ORDER BY Timestamp DESC",
    new { ObjectDn = objectDn }(),
);
```

### Read-Only Attributes (Skip on Restore)

These attributes cannot be modified and must be excluded from restore operations:

- `objectGuid`, `objectSid`, `whenCreated`, `whenChanged`
- `uSNCreated`, `uSNChanged`, `objectCategory`
- `distinguishedName`, `cn` (use ModifyDN for moves instead)

### File Layout

- `Models/ObjectSnapshot.rs`
- `Services/Snapshots/SnapshotService.rs`
- `Services/Snapshots/SnapshotService.rs`
- `Data/SnapshotDatabase.rs` - SQLite initialization and connection factory

### Testing

- Use in-memory SQLite database for tests (`Data Source=:memory:`)
- Use `mockall` (Rust) or vitest mocks to mock `DirectoryProvider` trait, `DialogService` trait, `PermissionService` trait.
- Use standard Rust or TypeScript assertions for readable assertions.
- Test file: `src-tauri/tests/Services/Snapshots/SnapshotServiceTests.rs`

## Change Log

| Date       | Version | Description                                                            | Author    |
| ---------- | ------- | ---------------------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                                       | Romain G. |
| 2026-03-13 | 1.1     | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
