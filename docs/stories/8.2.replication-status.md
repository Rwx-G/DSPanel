# Story 8.2: AD Replication Status

## Status: Draft

## Story

**As a** DomainAdmin,
**I want** to view the status of all AD replication partnerships and force replication when needed,
**so that** I can monitor replication health and resolve sync issues proactively.

## Acceptance Criteria

1. A "Replication" view shows all replication partnerships with source DC, target DC, last successful sync, status, and error count.
2. Failed replication partnerships are highlighted in red with error details.
3. Replication latency (time since last successful sync) is displayed for each partnership.
4. A "Force Replication" button triggers replication sync for a selected partnership, with a confirmation dialog.
5. The view auto-refreshes at a configurable interval (default: 120 seconds).
6. The Replication view is gated to DomainAdmin permission level.
7. Unit tests cover partnership listing, error highlighting, force replication, and permission gating.

## Tasks / Subtasks

- [ ] Task 1: Query replication partnerships (AC: #1)
  - [ ] Query `msDS-ReplNeighbor` attribute via LDAP on each DC's NTDS Settings object
  - [ ] Alternatively, parse output of `repadmin /showrepl` if LDAP query is insufficient
  - [ ] Create `ReplicationPartnership` model: `SourceDc`, `TargetDc`, `NamingContext`, `LastSyncTime`, `LastSyncResult`, `ConsecutiveFailures`, `LastSyncMessage`
- [ ] Task 2: Create replication view (AC: #1, #2, #3)
  - [ ] `ReplicationStatusView.xaml` with DataGrid or card layout
  - [ ] Columns: Source DC, Target DC, Naming Context, Last Sync, Status, Errors
  - [ ] Sort by status (failed first) or by last sync time
  - [ ] Auto-refresh timer with configurable interval
- [ ] Task 3: Partnership cards with error highlighting (AC: #2)
  - [ ] Red background/border for partnerships with `LastSyncResult != 0`
  - [ ] Show error message and consecutive failure count
  - [ ] Tooltip or expandable section with detailed error information
- [ ] Task 4: Latency display (AC: #3)
  - [ ] Compute latency as `DateTime.UtcNow - LastSyncTime`
  - [ ] Display as human-readable duration ("2 minutes ago", "3 hours ago")
  - [ ] Color code: green (< 15 min), yellow (15 min - 1 hr), red (> 1 hr)
- [ ] Task 5: Force replication (AC: #4)
  - [ ] Implement `ForceReplicationAsync(sourceDc, targetDc, namingContext)` using `DsReplicaSync` via P/Invoke or `repadmin /replicate`
  - [ ] Confirmation dialog via `IDialogService` before execution
  - [ ] Show success/failure result after force replication
  - [ ] Refresh the partnership status after sync attempt
- [ ] Task 6: Permission gating (AC: #6)
  - [ ] Gate Replication view to DomainAdmin via `IPermissionService`
  - [ ] Hide navigation entry for lower permission levels
- [ ] Task 7: Write tests (AC: #7)
  - [ ] Test partnership data parsing from LDAP or repadmin output
  - [ ] Test error highlighting logic
  - [ ] Test latency computation and color coding
  - [ ] Test force replication calls correct method with confirmation
  - [ ] Test auto-refresh triggers data reload
  - [ ] Test permission gating

## Dev Notes

### Replication Data Sources

Option A - LDAP query of `msDS-ReplNeighbor`:
```csharp
var request = new SearchRequest(
    "", // RootDSE
    "(objectClass=*)",
    SearchScope.Base,
    "msDS-ReplNeighbor"
);
```

Option B - Parse `repadmin /showrepl /csv`:
```csharp
var process = Process.Start(new ProcessStartInfo
{
    FileName = "repadmin",
    Arguments = "/showrepl * /csv",
    RedirectStandardOutput = true,
    UseShellExecute = false
});
var csvOutput = await process.StandardOutput.ReadToEndAsync();
```

Option B is simpler and provides a comprehensive view across all DCs.

### DsReplicaSync

Force replication can be triggered via:
- P/Invoke to `DsReplicaSync` from `ntdsapi.dll`
- Running `repadmin /replicate <destDC> <sourceDC> <namingContext>`

The `repadmin` approach is simpler and does not require P/Invoke declarations.

### File Layout

- `ViewModels/ReplicationStatusViewModel.cs`
- `Views/Pages/ReplicationStatusView.xaml`
- `Models/ReplicationPartnership.cs`
- `Services/Health/IReplicationService.cs`
- `Services/Health/ReplicationService.cs`

### Testing

- Use `Moq` to mock `IReplicationService`, `IDialogService`, `IPermissionService`.
- Use `FluentAssertions` for readable assertions.
- Test file: `src/DSPanel.Tests/Services/Health/ReplicationServiceTests.cs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
