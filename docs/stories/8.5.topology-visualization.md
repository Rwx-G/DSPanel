# Story 8.5: AD Topology Visualization

## Status: Draft

## Story

**As a** DomainAdmin,
**I want** a visual graph showing AD sites, domain controllers, and replication links,
**so that** I can understand the replication topology at a glance and identify connectivity issues.

## Acceptance Criteria

1. A topology graph displays sites as regions/containers, DCs as nodes within their sites, and replication links as edges between DCs.
2. Replication link edges are color-coded by status: green (healthy), yellow (warning), red (failed).
3. Site link costs and replication intervals are displayed on edges connecting sites.
4. Nodes and edges are interactive - clicking opens a detail panel with full information.
5. The graph supports zoom and pan for large topologies.
6. The topology can be exported as a PNG image.
7. The topology view is gated to DomainAdmin permission level.
8. Unit tests cover data model construction, status computation, and export logic.

## Tasks / Subtasks

- [ ] Task 1: Query AD Sites and Services via LDAP (AC: #1)
  - [ ] Query `CN=Sites,CN=Configuration,<forest DN>` for site objects
  - [ ] Query server objects within each site for DCs
  - [ ] Query `CN=IP,CN=Inter-Site Transports,CN=Sites,CN=Configuration,<forest DN>` for site links
  - [ ] Query NTDS Connection objects for intra-site replication links
  - [ ] Create `TopologyData` model containing sites, DCs, and links
- [ ] Task 2: Create topology data model (AC: #1, #2, #3)
  - [ ] `SiteNode`: Name, Location, list of DcNodes
  - [ ] `DcNode`: Hostname, SiteName, IsPdc, IsGc, position coordinates
  - [ ] `ReplicationLink`: SourceDc, TargetDc, Status, LastSyncTime, ErrorCount
  - [ ] `SiteLink`: Sites (list), Cost, ReplicationInterval
- [ ] Task 3: Implement graph rendering (AC: #1, #2, #3)
  - [ ] Use React canvas component with custom drawing, or evaluate a React graph library library (e.g., react-force-graph, D3.js)
  - [ ] Render sites as labeled rectangles/regions
  - [ ] Render DCs as labeled circles/icons within site regions
  - [ ] Render replication links as colored lines/arrows between DCs
  - [ ] Render site links as dashed lines between site regions with cost/interval labels
  - [ ] Apply automatic layout algorithm for node positioning
- [ ] Task 4: Interactive nodes and edges (AC: #4)
  - [ ] Click on a DC node to show: hostname, IP, FSMO roles, OS version, site membership
  - [ ] Click on a replication link to show: last sync, status, error details, naming context
  - [ ] Click on a site link to show: cost, interval, connected sites, schedule
  - [ ] Detail panel appears as a side panel or popup
- [ ] Task 5: Zoom and pan (AC: #5)
  - [ ] Implement mouse wheel zoom on the Canvas
  - [ ] Implement click-and-drag pan
  - [ ] Add zoom controls (zoom in, zoom out, fit to view)
  - [ ] Maintain minimum and maximum zoom levels
- [ ] Task 6: PNG export (AC: #6)
  - [ ] Render the Canvas content to a HTML Canvas export
  - [ ] Save as PNG via `Canvas toBlob API`
  - [ ] File save dialog for output location
  - [ ] Export at current zoom level or fit-to-view
- [ ] Task 7: Permission gating (AC: #7)
  - [ ] Gate topology view to DomainAdmin via `PermissionService` trait
  - [ ] Hide navigation entry for lower permission levels
- [ ] Task 8: Write tests (AC: #8)
  - [ ] Test topology data model construction from LDAP query results
  - [ ] Test replication link status color computation
  - [ ] Test site link cost/interval display formatting
  - [ ] Test layout algorithm produces valid coordinates
  - [ ] Test PNG export produces valid image bytes
  - [ ] Test permission gating

## Dev Notes

### AD Sites and Services LDAP Structure

```
CN=Sites,CN=Configuration,DC=contoso,DC=com
  CN=Default-First-Site-Name          (site)
    CN=Servers
      CN=DC1                          (server)
        CN=NTDS Settings              (nTDSDSA - has connection objects)
  CN=Inter-Site Transports
    CN=IP
      CN=DEFAULTIPSITELINK             (siteLink)
```

### Key LDAP Attributes

| Object | Attributes |
|--------|-----------|
| Site | `cn`, `location`, `siteObjectBL` |
| Server | `cn`, `dNSHostName`, `serverReference` |
| NTDS Settings | `objectGUID`, `options` (isGC flag), `msDS-HasMasterNCs` |
| NTDS Connection | `fromServer`, `schedule`, `options` |
| Site Link | `siteList`, `cost`, `replInterval` |

### Graph Layout

For automatic layout, consider:
- **Force-directed layout**: Good for general graphs, may not respect site grouping
- **Hierarchical layout**: Sites as containers with DCs arranged inside
- **Manual positioning**: Sites placed in a grid, DCs within each site cell

The hierarchical/container approach is recommended to visually group DCs by site.

### PNG Export

```typescript
// Export canvas content to PNG using the HTML Canvas API
const canvas = canvasRef.current;
canvas.toBlob((blob) => {
  if (blob) {
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = outputFilename;
    link.click();
    URL.revokeObjectURL(url);
  }
}, 'image/png');
```

### File Layout

- `components/TopologyComponent.tsx`
- `Views/Pages/TopologyView.tsx`
- `Models/Topology/SiteNode.rs`
- `Models/Topology/DcNode.rs`
- `Models/Topology/ReplicationLink.rs`
- `Models/Topology/SiteLink.rs`
- `Models/Topology/TopologyData.rs`
- `Services/Health/TopologyService.rs`
- `Services/Health/TopologyService.rs`

### Testing

- Use `mockall` (Rust) or vitest mocks to mock `TopologyService` trait, `PermissionService` trait.
- Use standard Rust or TypeScript assertions for readable assertions.
- Test file: `src-tauri/tests/Services/Health/TopologyServiceTests.rs`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial creation | Romain G. |
| 2026-03-13 | 1.1 | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
