# Story 9.1: Privileged Accounts Dashboard

## Status: Done

## Story

**As a** DomainAdmin,
**I want** a dashboard listing all privileged accounts with health indicators and alerts,
**so that** I can quickly identify high-risk accounts and take corrective action.

## Acceptance Criteria

1. The dashboard lists members of Domain Admins, Enterprise Admins, Schema Admins, and any additional configurable groups.
2. Each account row shows: SAMAccountName, display name, last logon, password age, password expiry date, and enabled/disabled status.
3. Alerts are raised for: password older than 90 days, password set to never expire, account never logged on, and disabled accounts still in a privileged group.
4. Each alert has a severity level (Critical, High, Medium, Info) displayed with color coding.
5. The dashboard provides CSV and PDF export of the full account list including alert details.
6. The view is gated to DomainAdmin permission level only.
7. Unit tests cover the React component, alert computation logic, and permission gating.

## Tasks / Subtasks

- [x] Task 1: Create `SecurityDashboardComponent` (AC: #1, #2, #3, #4)
    - [x] Add `PrivilegedAccounts` React state array with account properties
    - [x] Add `Alerts` React state array with severity, message, and target account
    - [x] Add `ConfigurableGroups` list loaded from settings
    - [x] Implement `LoadPrivilegedAccountsCommand` (async event handler)
- [x] Task 2: Query privileged group members via `DirectoryProvider` trait (AC: #1, #2)
    - [x] Add `GetGroupMembersAsync(string groupDn, bool recursive)` if not already on `DirectoryProvider` trait
    - [x] Query Domain Admins, Enterprise Admins, Schema Admins, and configured groups
    - [x] Retrieve lastLogonTimestamp, pwdLastSet, userAccountControl for each member
- [x] Task 3: Compute alerts (AC: #3, #4)
    - [x] Create `PrivilegedAccountAlertCalculator` service
    - [x] Implement password age > 90 days detection (Critical)
    - [x] Implement password never expires detection (High)
    - [x] Implement never logged on detection (Medium)
    - [x] Implement disabled account in privileged group detection (High)
- [x] Task 4: Create `SecurityDashboardView.tsx` (AC: #1, #2, #3, #4)
    - [x] Alert summary cards at the top (count by severity)
    - [x] React data table with account details and inline alert indicators
    - [x] Color-coded severity badges
    - [x] Configurable groups settings panel
- [x] Task 5: Export functionality (AC: #5)
    - [x] Add export buttons (CSV, PDF) calling `ExportService` trait
    - [x] Include alert details in export output
- [x] Task 6: Permission gating (AC: #6)
    - [x] Gate the Security Dashboard navigation item to DomainAdmin
    - [x] Hide from lower permission levels
- [x] Task 7: Write tests (AC: #7)
    - [x] Test alert calculation for each condition
    - [x] Test severity assignment logic
    - [x] Test React component loads and populates collections
    - [x] Test permission gating hides view for non-DomainAdmin
    - [x] Test export triggers with correct data

## Dev Notes

### Privileged Group Resolution

The default privileged groups are identified by well-known SIDs: Domain Admins (S-1-5-21-...-512), Enterprise Admins (S-1-5-21-...-519), Schema Admins (S-1-5-21-...-518). Additional groups are configurable via settings and stored as distinguished names.

### Alert Severity Levels

| Condition                            | Severity |
| ------------------------------------ | -------- |
| Password > 90 days old               | Critical |
| Password set to never expire         | High     |
| Disabled account in privileged group | High     |
| Account never logged on              | Medium   |

### File Layout

- `components/Security/SecurityDashboardComponent.tsx`
- `Views/Security/SecurityDashboardView.tsx`
- `Services/Security/PrivilegedAccountAlertCalculator.rs`
- `Models/Security/PrivilegedAccountInfo.rs`
- `Models/Security/SecurityAlert.rs`

### Testing

- Use `mockall` (Rust) or vitest mocks to mock `DirectoryProvider` trait, `PermissionService` trait, and `ExportService` trait.
- Use standard Rust or TypeScript assertions for readable assertions.
- Test file: `src-tauri/tests/components/Security/SecurityDashboardComponentTests.tsx`
- Test file: `src-tauri/tests/Services/Security/PrivilegedAccountAlertCalculatorTests.rs`

## Change Log

| Date       | Version | Description                                                            | Author    |
| ---------- | ------- | ---------------------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                                       | Romain G. |
| 2026-03-13 | 1.1     | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |

## QA Results

### Review Date: 2026-03-23
### Reviewed By: Quinn (Test Architect)
### Gate: PASS (95/100)

All acceptance criteria met. Backend service queries privileged groups with comprehensive alert detection (password age, never expires, disabled in priv group, never logged on). Frontend displays accounts table with severity badges, expandable rows, and CSV export. DomainAdmin gating enforced at command level.

Tests: 21 Rust + 15 frontend = 36 total
Gate file: docs/qa/gates/9.1-privileged-accounts.yml
