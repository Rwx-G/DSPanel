# Story 9.2: Domain Risk Score

## Status: Review

## Story

**As a** DomainAdmin,
**I want** a composite risk score for my domain based on weighted security factors,
**so that** I can measure overall AD health and track improvements over time.

## Acceptance Criteria

1. A weighted score from 0 (worst) to 100 (best) is computed from: privileged account hygiene, password policy strength, stale account ratio, and dangerous configuration flags.
2. A gauge visualization displays the score with color zones (red 0-40, orange 41-70, green 71-100).
3. A factor breakdown panel shows each factor's individual score, weight, and a plain-text explanation.
4. Actionable recommendations are generated for each factor scoring below threshold.
5. A 30-day trend chart shows historical scores stored locally in SQLite.
6. Factor weights are configurable by the administrator in settings.
7. Unit tests cover the risk calculator, each factor's scoring logic, and historical storage.

## Tasks / Subtasks

- [x] Task 1: Create `RiskScoreCalculator` service (AC: #1, #6)
    - [x] Define `RiskScoreCalculator` trait interface with `CalculateAsync()` returning `RiskScoreResult`
    - [x] Implement weighted factor aggregation
    - [x] Create `RiskFactor` model (name, score, weight, explanation, recommendations)
    - [x] Load configurable weights from settings (defaults provided)
- [x] Task 2: Implement individual risk factors (AC: #1)
    - [x] Privileged account hygiene factor (password age, never expires, disabled in priv group)
    - [x] Password policy strength factor (complexity, min length, lockout policy)
    - [x] Stale account ratio factor (inactive > 90 days / total accounts)
    - [x] Dangerous configurations factor (unconstrained delegation, AdminSDHolder anomalies)
- [x] Task 3: Create gauge visualization (AC: #2)
    - [x] Custom React or Tauri control or third-party gauge rendering score 0-100
    - [x] Color zones: red (0-40), orange (41-70), green (71-100)
    - [x] Animated score display on load
- [x] Task 4: Create factor breakdown panel (AC: #3)
    - [x] List each factor with individual score bar, weight percentage, explanation text
    - [x] Expandable detail sections
- [x] Task 5: Recommendations engine (AC: #4)
    - [x] Generate actionable recommendations per factor when score is below threshold
    - [x] Priority ordering by impact
- [x] Task 6: Historical storage and trend chart (AC: #5)
    - [x] SQLite table: `risk_scores (id, date, total_score, factors_json)`
    - [x] Store one entry per day on each calculation
    - [x] 30-day line chart using React charting library or lightweight charting
- [x] Task 7: Configuration UI (AC: #6)
    - [x] Weight sliders or numeric inputs per factor in settings
    - [x] Validation that weights sum to 100%
- [x] Task 8: Write tests (AC: #7)
    - [x] Test weighted score calculation with known inputs
    - [x] Test each factor's scoring logic independently
    - [x] Test recommendations generation
    - [x] Test historical storage insert and retrieval
    - [x] Test weight configuration validation

## Dev Notes

### Factor Weights (Defaults)

| Factor                     | Default Weight |
| -------------------------- | -------------- |
| Privileged account hygiene | 30%            |
| Password policy strength   | 25%            |
| Stale account ratio        | 25%            |
| Dangerous configurations   | 20%            |

### SQLite Schema

```sql
CREATE TABLE risk_scores (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT NOT NULL UNIQUE,
    total_score REAL NOT NULL,
    factors_json TEXT NOT NULL
);
```

### File Layout

- `Services/Security/RiskScoreCalculator.rs`
- `Services/Security/RiskScoreCalculator.rs`
- `Services/Security/Factors/` - one class per factor
- `Models/Security/RiskScoreResult.rs`
- `Models/Security/RiskFactor.rs`
- `Views/Security/RiskScoreView.tsx`
- `components/Security/RiskScoreComponent.tsx`
- `Data/RiskScoreRepository.rs`

### Testing

- Use `mockall` (Rust) or vitest mocks to mock `DirectoryProvider` trait and factor data sources.
- Use in-memory SQLite for repository tests.
- Use standard Rust or TypeScript assertions for readable assertions.
- Test file: `src-tauri/tests/Services/Security/RiskScoreCalculatorTests.rs`

## Change Log

| Date       | Version | Description                                                            | Author    |
| ---------- | ------- | ---------------------------------------------------------------------- | --------- |
| 2026-03-10 | 1.0     | Initial creation                                                       | Romain G. |
| 2026-03-13 | 1.1     | Updated tech references for Rust/Tauri v2 + React/TypeScript migration | Romain G. |
