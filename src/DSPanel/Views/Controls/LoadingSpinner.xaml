<UserControl x:Class="DSPanel.Views.Controls.LoadingSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid x:Name="PART_Root" Visibility="Collapsed"
        Background="{DynamicResource BrushSurfaceOverlay}">
    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">
      <!-- Spinning circle -->
      <Ellipse x:Name="PART_Spinner"
               Width="32" Height="32"
               StrokeThickness="3"
               Stroke="{DynamicResource BrushPrimary}"
               StrokeDashArray="3 2"
               RenderTransformOrigin="0.5,0.5">
        <Ellipse.RenderTransform>
          <RotateTransform x:Name="SpinnerRotation" Angle="0"/>
        </Ellipse.RenderTransform>
      </Ellipse>

      <!-- Optional message -->
      <TextBlock x:Name="PART_Message"
                 Style="{StaticResource Body}"
                 Foreground="{DynamicResource BrushTextSecondary}"
                 HorizontalAlignment="Center"
                 Margin="0,12,0,0"
                 Visibility="Collapsed"/>
    </StackPanel>

    <Grid.Triggers>
      <EventTrigger RoutedEvent="Loaded">
        <BeginStoryboard x:Name="SpinStoryboard">
          <Storyboard RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="SpinnerRotation"
                             Storyboard.TargetProperty="Angle"
                             From="0" To="360"
                             Duration="0:0:1"/>
          </Storyboard>
        </BeginStoryboard>
      </EventTrigger>
    </Grid.Triggers>
  </Grid>
</UserControl>
