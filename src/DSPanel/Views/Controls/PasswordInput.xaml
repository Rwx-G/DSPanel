<UserControl x:Class="DSPanel.Views.Controls.PasswordInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Border x:Name="PART_OuterBorder"
          Background="{DynamicResource BrushSurfaceCard}"
          BorderBrush="{DynamicResource BrushBorderDefault}"
          BorderThickness="1"
          CornerRadius="{StaticResource BorderRadiusMd}">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <!-- Hidden PasswordBox (default) -->
      <PasswordBox x:Name="PART_PasswordBox"
                   Grid.Column="0"
                   Background="Transparent"
                   Foreground="{DynamicResource BrushTextPrimary}"
                   BorderThickness="0"
                   FontSize="14"
                   FontFamily="Segoe UI"
                   Padding="8,6,0,6"
                   PasswordChanged="OnPasswordChanged"/>

      <!-- Visible TextBox (when showing) - override global style to remove its own border -->
      <TextBox x:Name="PART_TextBox"
               Grid.Column="0"
               Visibility="Collapsed"
               Background="Transparent"
               BorderThickness="0"
               FontSize="14"
               FontFamily="Segoe UI"
               Padding="8,6,0,6"
               CaretBrush="{DynamicResource BrushTextPrimary}"
               Text="{Binding Password, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        <TextBox.Style>
          <Style TargetType="TextBox">
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                  <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </TextBox.Style>
      </TextBox>

      <!-- Toggle button (inline template: no hover background, opacity change only) -->
      <Button x:Name="PART_ToggleButton"
              Grid.Column="1"
              Width="28" Height="28"
              Padding="0"
              Margin="0,0,4,0"
              VerticalAlignment="Center"
              Click="OnToggleVisibility"
              Cursor="Hand"
              AutomationProperties.Name="Toggle password visibility"
              ToolTip="Show/Hide password">
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <Border Background="Transparent" CornerRadius="4">
              <Path x:Name="icon"
                    Data="{StaticResource IconEye}"
                    Width="16" Height="16"
                    Stretch="Uniform"
                    Fill="{DynamicResource BrushTextSecondary}"
                    Opacity="0.5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="icon" Property="Opacity" Value="1.0"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Button.Template>
      </Button>
    </Grid>
  </Border>
</UserControl>
